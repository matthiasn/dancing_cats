
======================================================================
SHAKU  mean 6  min 5.5

  VERDICT afrobeats: The crossed-wrist X finally holds as the real Shaku Shaku base posture over a staggered step, but a camera-square, non-leaning torso and a generic hands-on-hips accent keep it from reading as groove-authentic Lagos street shaku.
  VERDICT movement: A leg exercise wearing a frozen torso: the crossed-arm hold never breathes, the promised off-axis lean never happens, and the beat-4 scoop is too small and too brief to land as punctuation.
  VERDICT director: The gallop footwork sells energy, but the crossed-arm hold never breathes and the promised off-axis lean never shows up, so it reads as busy legs under a frozen torso rather than a full-body Shaku accent.
  VERDICT anatomy: A legible, well-staggered crossed-arm gallop whose held "X" keeps visibly collapsing into a fused hand-blob and whose spine never banks into its own crossed stance.
  VERDICT rigging: Gallop footwork and tail read clean, but the signature crossed-wrist X — the pose held for most of the loop — renders as a rigging glitch on the hero shot, and the punch-out accent loses arm articulation entirely; not sign-off ready.
  [afrobeats] (f12) The transient 'open' accent (the punctuation moment that should be the freestyle flourish) reads as a generic hands-on-hips/akimbo plant, not an actual Shaku Shaku gesture — it looks like stock boy-band posing rather than the open scoop or a generator-pull.
      FIX: Swap the punctuation key for a real open scoop: both forearms sweep outward/upward from the crossed centre with a wrist snap, staying close to the torso, or use the generator-pull (one arm rips down-and-back like starting a cord) that the research audit flags as native to this slot.
  [afrobeats] (f0) The torso is bolt upright and camera-square for the entire 32-frame loop even though off-axis lean is explicitly part of this move's own brief — a Lagos/Accra viewer would clock this as stiff, not loose-limbed street shaku.
      FIX: Add an 8-12 degree spine/ribcage tilt that alternates with the staggered step so the upper body rocks opposite the lead foot, instead of staying vertical while only the legs and arms move.
  [afrobeats] (f4) No vertical bob or settle is visible across the staggered gallop step — head height stays essentially flat through the phrase even where the legs are crossing hard (e.g. the crossed-leg stance here), so the gallop reads as a shuffle-in-place rather than a bouncy half-gallop.
      FIX: Add a small harmonic-4 root-Y dip synced to the off-beat skips so each staggered step produces a visible settle-and-lift, giving the gallop its characteristic bounce.
  [movement] (f16) The onion-skin overlay of the full 32-frame loop shows the head/torso/crossed-arm block barely displacing at all while the legs fan through a wide staggered arc — the upper body is a rigid unit riding passively on top of independently working legs rather than a single kinetic chain. Confirmed numerically too: frame-to-frame pixel diffs are flat and low everywhere except the two scoop transitions, meaning the torso contributes nothing to the phrasing.
      FIX: Drive the chest/shoulder block off the same weight-transfer signal that moves the legs: a small vertical bob and lateral shoulder rock timed to each staggered plant, so the X-hold visibly rides the step's impulse instead of floating above it.
  [movement] (f20) The intended vocabulary explicitly calls for off-axis lean, but through every stance — including the deepest staggered lunges — the spine and head stay dead vertical and centered over the pelvis. There is no ribcage tip toward the weighted leg anywhere in the loop.
      FIX: Bank the spine 8-12 degrees off vertical toward the grounded/weighted leg at each staggered plant (with a small counter-tilt in the head to keep it readable as intentional lean, not a rig error), easing back to neutral as weight redistributes.
  [movement] (f12) The beat-4 scoop opens on-beat but the fists barely clear the chest before recrossing about a frame later — at this amplitude and duration it reads as a flicker, not the transient punctuation the move calls for. It's also weaker and more symmetric than the beat-8 scoop later in the same loop, which reaches further and asymmetrically, so the two accents don't read as the same gesture.
      FIX: Match the beat-4 scoop's arm extension to the beat-8 version's reach (hands should clear the torso silhouette by the same margin) and hold the open position one extra frame before recrossing so the accent has time to register.
  [director] (f8) The upper body is essentially one frozen pose for most of the 8-beat phrase — the crossed-wrist X at frame 0 is still nearly pixel-identical at frames 4, 8, 16, 20, 24, with only the legs shifting stance. Only two brief windows (frames 12-13 and 28-29) break the hold, and they're mirror-copies of each other landing on the exact same beat offset (beat 3 and beat 7), so the whole loop has just one real accent repeated twice.
      FIX: Give the held X a beat-synced breathing motion — small forearm counter-rotation or shoulder shrug on the off-beats — so the base posture is visibly alive even when it isn't scooping, and stagger the two scoop accents to different beats/shapes instead of a symmetric mirror repeat.
  [director] (f12) The brief calls for off-axis lean, but in every sampled cell — including both open-scoop punctuation moments at frame 12 and frame 28 — the head and spine stay dead vertical and frontal to camera. Only the legs move; the torso never banks.
      FIX: Bank the spine and head 10-15 degrees toward the lead foot on the gallop weight shift, and push the lean further on the scoop hits, so the silhouette visibly tips off-axis instead of staying a static forward-facing torso over moving feet.
  [director] (f0) The crossed-wrist X, the pose's whole visual signature, is hard to read at a glance: the forearms are the same navy as the suit sleeves and pulled tight against the ribcage, so at small screen size only the two small orange fists are legible and the actual crossed shape disappears into the jacket silhouette.
      FIX: Push the elbows out away from the ribcage to open the X, and/or add a lighter cuff or contrast trim on the sleeves so the crossed-arm graphic reads as a clear shape against the dark jacket rather than blending into it.
  [anatomy] (f25) The crossed-wrist X, which is supposed to be the rock-solid base posture, is not actually stable frame to frame — on the tween frames between keyframes (clean at frames 0, 8, 16, 24) the two fists periodically fuse into a single teardrop mass with the cuffs pushed out to the sides, i.e. the wrists pass through each other's volume rather than sliding past in a clean crossed grip.
      FIX: Key the crossed-hand sub-pose explicitly at every frame (or enforce a minimum wrist-to-wrist center offset in the IK/tween solve) so the interpolation path between keyframes never lets the two hand meshes fully overlap.
  [anatomy] (f4) At the deepest cross-step (legs maximally staggered/crossed) the spine stays bolt upright and centered directly over the base instead of banking toward the weighted leg, which is both a balance problem — a real body can't stay dead-vertical over that narrow crossed base without falling — and a miss on the brief's requested off-axis lean.
      FIX: Drive a spine-lean channel from the current lateral foot separation/crossing amount so the ribcage banks a few degrees toward the grounded leg during the deepest cross frames, coordinating spine with hip position instead of holding the torso rigid.
  [anatomy] (f28) In the open-scoop punctuation the reaching arm shows zero elbow break — the upper arm and forearm render as one uniform-width straight ribbon from shoulder to fist, so the joint reads as a single rigid rod pivoting only at the shoulder rather than an articulated shoulder-elbow-wrist chain.
      FIX: Enforce a minimum elbow flexion (roughly 15-20 degrees) whenever the arm extends past horizontal, and taper the ribbon mesh so the elbow midpoint is visually distinct from the upper-arm and forearm segments.
  [rigging] (f0) The crossed-wrist X base posture (held through nearly the whole 32-frame loop per the onion-skin) breaks down at the sternum: the tie renders as two disconnected maroon fragments with a gap of bare collar between them, and 3-4 stray thin black seam/wireframe strokes fan out across the white collar where the two forearm ribbons overlap the torso — visible as a rendering glitch, most damning in the lit stage view where it's front-and-center on the hero cat.
      FIX: Draw the tie as one unbroken path clipped only against the true outer silhouette of the crossed arms, not against internal ribbon-segment edges; z-order/cull the internal mesh boundary strokes so only the outermost contour of the overlapping sleeves gets an outline stroke.
  [rigging] (f28) In the open-scoop punch-out accent the extended forearm and fist fuse into a single flat, untapered paddle/flipper shape with no visible elbow bend or wrist break, and its outer edge silhouette-tangents into the jacket lapel, reading as an extra lobe growing off the torso rather than an articulated arm.
      FIX: Reinstate width-tapering at the elbow and wrist control points on the ribbon spline for this pose, and pull the taper inboard of the torso outline so the arm silhouette separates cleanly from the lapel instead of fusing to it.
  [rigging] (f4) During the crossed-arm hold the two orange fists sit at nearly coincident screen position/depth every frame, collapsing into one indistinct round blob rather than a legible over/under stack — the 'X' only reads through two thin cuff-seam lines, not through the actual hand shapes, undercutting the move's signature silhouette.
      FIX: Offset the two hand meshes in screen position and draw order so one fist visibly sits proud of the other with a clear occlusion edge, giving the crossing a readable stacked read instead of a flush merged blob.

======================================================================
ZANKU  mean 5.9  min 5.5

  VERDICT afrobeats: The legwork skeleton — forward lean, rib-guard fists, tap-dig-scrape cadence — is genuinely Zanku, but the namesake air-kick never kicks and the face/shoulders carry zero attitude, so it reads as a competent rehearsal run-through rather than the real gbese.
  VERDICT movement: The legwork winds up before the beat and goes flat exactly on it, while the arms sit parked in akimbo the whole phrase — so the one thing "zanku" is supposed to sell (heel-toe accent + arm punctuation) never actually reads on the beat.
  VERDICT director: The legwork reads as a generic cross-step knee-lift on a loop, not zanku's heel-toe scrape, and the arms never do the pump that was supposed to punctuate it — so the move under-delivers on both halves of its own brief.
  VERDICT anatomy: The legwork this move is named for never actually articulates — it alternates between two locked-knee poses with clipping crossed feet, while the "elbows-out pump" is too small to punctuate against it.
  VERDICT rigging: Solid neutral pose, clean silhouette at rest, and no gross floor sliding — but the cross-body twist pose that recurs every cycle pinches the sleeve into a spike and piles the feet together, which is a repeating rig defect, not a one-off, and would come back in every review pass.
  [afrobeats] (f28) The signature Zanku air-kick ("gbese") tops out at shin/ankle height with the torso staying upright and vertical the whole time — no lean-back at the peak. Any Lagos/Accra viewer clocks the namesake move of the whole style as the weakest beat in the phrase, not the highlight.
      FIX: Raise the kick apex so the shin clears at least hip height with the foot flicking outward, and add a torso lean-back of roughly 10-15 degrees timed only to the 2-3 frame peak (return to forward carriage immediately after) so the kick reads as a release, not a tap.
  [afrobeats] (f0) The head stays level and forward with wide eyes and an open, slightly surprised mouth for the entire 32-frame loop. Real Zanku carries a bent-forward back with a dropped chin, eyes toward the floor tracking the footwork — that downward focus is part of what sells the legwork as effortful, controlled damage rather than a happy bounce.
      FIX: Add a chin-down head channel that dips 8-12 degrees and holds through the tap-dig-scrape bars, only lifting to neutral at the kick peak or a punctuation beat, and swap the default surprised-mouth expression for a closed-mouth focused or smirking one.
  [afrobeats] (f12) The arms are locked into one static rib-guard akimbo shape for the whole phrase with only a single forearm crossing over for the scrape beat — there is no shoulder roll or elbow-pump channel, so the "vigorous shoulder rolls" that sources call out are simply absent and the punctuation feels stiff/robotic rather than snapping to the beat.
      FIX: Layer a small clavicle/shoulder roll (rotate shoulders back-to-front a few degrees) synced to the stomp beats underneath the existing rib-guard fist pose, so the arms visibly punctuate each stomp instead of sitting frozen between the one scrape gesture.
  [movement] (f4) Every beat frame (0, 4, 8, 12, 16, 20, 24, 28 — grid cells at col0/col3 of each row) shows the identical flat 'hands-on-hips, feet already settled' stance, while the leg-lift/twist that should be the stomp-dig accent happens one frame earlier (e.g. frame 3, 11, 19, 27) as a pre-beat wind-up. The energy dissipates into a neutral pose right as the beat lands — a dead arrival at the one moment the accent needs to read.
      FIX: Move the foot-plant/dig extreme itself onto the beat frame with a sharp weight-sink/ankle-snap on contact (slight overshoot then settle after the beat), instead of resolving to neutral before the beat hits.
  [movement] (f0) Arms are locked in a static hands-on-hips (akimbo) hold at every single beat across all 8 beats of the phrase; the only arm motion occurs off-beat during the lean/twist transit frames. The stated vocabulary is 'elbows-out arm pumps' punctuating the legwork, but the arms never visibly punctuate anything — they're a passive rest position the whole loop, confirmed in the live hero frame where the arms are inert while the face sells all the energy.
      FIX: Add a quick elbows-out pump (a few degrees of lift/widen at the elbow) timed to land on each beat alongside the foot accent, so the upper body visibly articulates instead of staying frozen in one held akimbo shape for 32 frames.
  [movement] (f20) Across the full 2-bar phrase, each 2-beat unit (one row of the grid) repeats the same plant-lean-kick-plant shape with near-identical amplitude — beat 5 (frame 20) and beat 7 (frame 28) read as copies of beat 1 (frame 0) and beat 3 (frame 12). There's no held beat, crescendo, or bigger accent marking a phrase turnaround, so the 2-bar loop plays like one 2-beat cell looped four times rather than a shaped phrase.
      FIX: Give one beat per bar (e.g. beat 4 or beat 8) a visibly larger scrape/dig or a brief suspended hold before the next cycle, so the ear's phrase structure (bar boundary) is answered by a distinct movement event instead of uniform repetition.
  [director] (f16) The arms are frozen in a hands-on-hips fist lock for all 32 frames — there is no elbow-pump punctuation at all, only passive shoulder tilt as the torso rotates with the weight shift.
      FIX: Layer a beat-synced elbow drive on top of the hip socket: elbows pull in tight on the 'and', snap out wide on the downbeat, so the arms visibly answer the legwork instead of sitting inert through the whole phrase.
  [director] (f28) The 8-beat/2-bar phrase is just the same 2-beat step-cross-kick cycle copy-pasted four times — the kick pose at frame 20 is essentially identical to the one at frame 28, and the same shape recurs again near frames 5 and 13. Nothing evolves across the phrase.
      FIX: Give the second bar a variation — swap kick direction, add a syncopated double-tap, or push the amplitude further on the back half — so the loop reads as a built phrase, not a stutter-repeat, when scrubbed at speed.
  [director] (f5) The signature zanku heel-toe tap-dig-scrape never shows: instead the working foot lifts clear off the ground into a pointed-toe passé/knee-hitch (e.g. the recurring lift at frame 5), which reads as a generic cross-step kick rather than the floor-hugging dig-and-scrape that defines this move.
      FIX: Keep the toe or heel in ground contact through the accent — lower the lift arc and add a short drag/slide translation on the contact foot before the stomp, so the cadence is legible as tap-dig-scrape-stomp rather than a hitch-kick.
  [anatomy] (f3) The recurring crossover/lean pose (repeats at frames 3, 11, 19, 27 — every 2 beats) shows both legs essentially straight at the knee while the torso leans ~30° and the feet cross so tightly the shoes overlap/clip through each other in silhouette.
      FIX: Bend both knees 15-25° during the cross and offset the feet laterally so the soles never occupy the same silhouette space — use the brief mid-transition knee flex already present at frame 1 as the target shape for the held pose, not just a passing tween.
  [anatomy] (f5) The wide 'stomp' stance (frames 0, 5, 8, 16, 20...) plants both legs fully extended at the knee for the whole ground-contact beat — a heel-toe-tap-dig-scrape-STOMP cadence needs a visible knee-bend at contact to sell weight transfer and impact, but here the legs read as stiff rods hinging only at the hip.
      FIX: Add 20-30° of knee flexion timed to land exactly on the stomp beat (loaded contact), then extend on the release — right now the knee is straighter at contact than in the transitions around it, which is backwards from how a real stomp loads.
  [anatomy] (f11) The elbows-out fists travel only from waist to low sternum across the entire 8-beat phrase and never clear the torso's silhouette (confirmed in zanku_live.png, where the pose reads as a static clench rather than a pump) — so the arms fail to punctuate the legwork as the brief requires.
      FIX: Raise the elbow above the shoulder line and widen the fist's lateral excursion on each accent beat so the pump silhouettes distinctly against the resting hips-pose, per the render-scale amplitude lesson (gestures must clear the silhouette, measured in pixels, not just joint angle).
  [rigging] (f3) In the torso-twist/cross-arm pose the two shoes overlap into a single merged blob at the ground rather than reading as a clean crossed step — a foot/shoe interpenetration, not a deliberate cross.
      FIX: Add a minimum ground-plane separation check (or offset the trailing foot's contact key) specifically for this crossed-step pose so the two shoe meshes never occupy the same footprint; verify against the identical pose at frame 11 which shows the same clash.
  [rigging] (f3) The crossing arm's sleeve ribbon pinches into a sharp pointed cusp jutting up beside the collar/neck instead of wrapping smoothly — this exact spike recurs identically every cycle (same pose at frames 11, 19, 27), so it's a systematic weight-painting artifact at the shoulder-collar junction, not noise.
      FIX: Add a relaxation pass or an extra control vertex at the shoulder/collar seam so the ribbon mesh doesn't collapse to a point when the humerus rotates across the chest; re-test this one pose in isolation since it repeats on every pass.
  [rigging] (f5) During the wide tap-dig arm pump the sleeve's top edge ripples into a triple-lobe scallop, visibly lumpier than the clean double-bump silhouette the same sleeve shows at rest (frame 0), reading as buckled fabric rather than a tailored blazer line.
      FIX: Add stiffness/damping (or reduce vertex density) at the shoulder cap of the sleeve ribbon so it doesn't over-articulate into extra scallops at high shoulder rotation.

======================================================================
AZONTO  mean 5.2  min 3.5

  VERDICT afrobeats: The steering-wheel and cross-body jabs described in the code never actually reach the screen — for nearly the whole 32-frame loop the cat just clutches a frozen crossed-forearm hug at the sternum, which a Lagos or Accra crowd will read as a cold-shiver/boxer's-guard tic, not Azonto.
  VERDICT movement: Bar 1 lands real accents on the beat but blurs its own pantomime, and bar 2 never actually dances its named vocabulary — arms hold one shape for four beats while only the legs and tail move.
  VERDICT director: The rig holds a static crossed-arm hug for nearly the whole loop with only the feet stepping — neither the steering wheel nor the jab ever appears in the silhouette.
  VERDICT anatomy: Bar 1's wheel-mime is inventive and lands its beat accents, but a one-frame kick pops in and out unrealistically and bar 2 never actually extends into a jab — needs a re-pass before this ships.
  VERDICT rigging: Solid wheel-mime hold and clean ribbons most of the loop, but a dropped-key foot pop and a recurring arm-fusion collapse on the signature cross-jab keep this below sign-off.
  [afrobeats] (f0) The signature mime is invisible: instead of two separated wheel grips trading height (brow/ribs), the hands solve to the exact 'frozen crossed guard' failure the code's own comments warn about — wrists pinned together at the sternum for ~5 of every 6 sampled cells across all 32 frames.
      FIX: The IK target data (x ±36-37) is already outside the flagged near-degenerate lane, so the fold is happening downstream in the two-bone/elbow solver or the mesh render, not in the authored keys — diff the rendered elbow/wrist screen positions against the target values frame-by-frame and add an explicit outboard elbow bias/pole vector so the grips visibly separate at the brow and rib heights instead of collapsing to center.
  [afrobeats] (f20) Bar 2's cross-body jab + idle-paw-chambered-at-hip contrast (the move's other half) doesn't read either — at the frame where the code calls for one arm at full extension past the opposite shoulder line and the other tucked at the hip crest, the render still shows the same central hug shape with no visible asymmetry or extension.
      FIX: Hold the JAB apex for at least 2 of the 4 frames in its beat (currently 1 frame at full tension before recoiling) and verify in the actual render — not just the keyframe table — that the jabbing wrist clears the opposite shoulder silhouette; right now the apex is being interpolated through too fast to register as a punch.
  [afrobeats] (f4) The one moment of large motion per beat-pair (the brief leg-kick/arm-throw break in the otherwise-frozen hug) reads as a stray stumble or off-balance lunge rather than a stylized steering or jab accent — it's visually the most jarring frame in the loop precisely because it's disconnected from any recognizable Azonto shape before or after it.
      FIX: Re-time this transient to land ON the wheel/jab pose it's supposed to be passing through (matching the authored key at that frame) rather than on an intermediate IK solve, and dial back the leg's forward reach so it doesn't read as a trip.
  [movement] (f4) The bar-1 accent that should read as gripping/turning a wheel instead reads as answering a phone: one paw holds a rigid rectangular shape up at the cheek while the other arm reaches out low and separate, so the signature mimetic image of the move is illegible at the exact moment it needs to be clearest.
      FIX: Keep both paws at wheel height and in the same rotational plane through the accent — replace the near-face fisted hand with a wheel-grip shape so the beat-1 extreme still reads unambiguously as 'turning a wheel', not a phone call.
  [movement] (f20) Bar 2's promised vocabulary (alternating cross-body jabs vs. an idle paw chambered at the hip) is not present in the silhouette: from frame 16 through frame 31 the arms stay locked in the same crossed-at-chest hold on every sampled frame, with only the stance and tail shifting. There is no jab extension breaking the body's midline, so the alternation and its weight/effort contrast never registers to the audience.
      FIX: Give the jabbing arm a genuine extension — elbow near-straight, fist crossing the centerline toward camera — on beats 4 and 6, and fully retract it to a hip-chambered pose on beats 5 and 7, so the two named actions produce two visibly different silhouettes that swap every beat.
  [movement] (f8) Around the beat-2-to-beat-3 span the wheel-mime pose freezes into an identical crossed-arm hold for roughly 1.5 beats, then snaps open at frame 12 and snaps closed again — a stop-motion hold/release pattern rather than the continuous rotary hand path a steering mime implies, which reads as isolated puppet-snaps instead of one body continuously turning a wheel.
      FIX: Replace the static hold with a continuous small elliptical path for both hands so beats 2 and 3 are just phase points on one ongoing rotation, not separate freeze-then-release events.
  [director] (f0) The 'steering a wheel' mime in bar 1 is illegible: both hands stay clamped together near the sternum with forearms crossed instead of gripping an implied wheel at shoulder width and arcing it — the silhouette reads as a hug, not driving.
      FIX: Separate the hands to roughly shoulder width, keep elbows out, and rotate both fists together left-right on the beat so the wheel-turn is visible in outline, not just in hand detail.
  [director] (f16) Bar 2's 'cross-body jabs vs. idle paw at hip' never differentiates from bar 1 — the arms are frozen in the same crossed-hug block for all 8 frames shown, with no arm extending outward as a jab and no chambered fist at the hip; bar 1 and bar 2 are visually the same pose.
      FIX: Give the jabbing arm a real forward strike (elbow drives past the ribs, hand leads, then snaps back) alternating sides on each beat, and pull the idle arm's elbow back with the fist pinned at the hip so the two arms read as opposite actions, not a mirrored clasp.
  [director] (f4) Across the full 32-frame loop only one beat (the lunge/kick) shows a real pose change; every other beat repeats the same torso/arm shape with legs doing all the work, so the move is monotonous well before the loop repeats and won't survive a small screen at a glance.
      FIX: Key a distinct upper-body silhouette on every beat (not just every other beat) and use the lunge frame's dynamic range as the amplitude floor for the rest of the phrase so no beat looks like a held pose.
  [anatomy] (f4) The beat-2 accent (rear-leg kick plus fist raised to the jaw) appears at frame 4 sandwiched between near-identical crossed-arm 'wheel grip' holds at frames 3 and 5 — i.e. the hip/knee/shoulder travel from a flat, weight-on-both-feet stance to full kick extension and back within a single 0.125s frame step on each side. No real body (or believable cartoon whip) can teleport a limb through that arc with zero anticipation or follow-through; it reads as a pop/glitch rather than a snap accent.
      FIX: Budget 2-3 in-between frames on each side of the frame-4 extreme: an anticipation frame where the knee starts to lift and the elbow begins to flex before frame 4, and a settling frame after it before returning to the frame-5 pose, so the joints are shown traversing the intermediate angles.
  [anatomy] (f12) In the raised-arm poses (clearest at frame 4, recurring at frame 12) the forearm/sleeve ribbon continues past the fist into a pointed flat blade, so the visible limb silhouette extends beyond the wrist joint — it reads as the cat gripping a rigid phone-like plank rather than a hand terminating the arm. This is a rigging/silhouette defect, not a deliberate prop, and it undercuts the wheel-mime readability it's supposed to sell.
      FIX: Clip or taper the forearm ribbon mesh at the wrist cuff so the fist polygon is always the terminal point of the arm silhouette, especially at the foreshortened/near-vertical forearm angles where this overshoot is worst.
  [anatomy] (f20) Across the whole of bar 2 (every sampled frame from 16-31) both forearms cross and both fists converge to the same point at the sternum and stay there, symmetric and static, elbows frozen at roughly the same ~110° flexion the entire bar. The brief calls for alternating cross-body jabs against an idle paw chambered at the hip, but no elbow ever extends into a reaching jab and no hand ever separates down to hip height — the extension end of the elbow's ROM is simply never used, so the intended punch action never mechanically happens.
      FIX: Alternate sides on 2-beat intervals: drive one elbow to near-full extension across the body (jab) while the other fist drops and tucks at the hip with the elbow flexed close to the torso, then swap the roles for the next phrase repeat.
  [rigging] (f4) Single-frame contact/volume glitch during the wheel mime: the support foot snaps onto a hyperextended tiptoe for one frame while the raised sleeve simultaneously flattens into a paper-thin blade near the ear, losing all tube volume — reads as a dropped or unsmoothed key rather than an eased accent.
      FIX: Add an in-between pose so the heel lift and the elbow's ribbon-width minimum are eased in/out over 2-3 frames instead of popping in one; clamp the sleeve's cross-section so it never collapses below a minimum width regardless of joint angle.
  [rigging] (f19) On the cross-body jab, both forearms/fists fuse into one indistinct blob directly over the chest and the tie collapses from its normal flag shape into a tiny stub swallowed by the hands — happens twice in the loop (once each cross-jab cycle) and defeats the intended 'jab + idle paw at hip' read by making both arms look chambered together.
      FIX: Enforce a minimum wrist-to-wrist separation in the arm solver so the two hand meshes can't coincide at the crossing extreme, and lock the tie's z-order/rest shape so it renders in front of both sleeves instead of being interpolated into the collapsed gap.
  [rigging] (f23) A stray black X-shaped line prints through the navy jacket surface below the crossed arms — an interior mesh/patch seam being stroked with outline color instead of suppressed, scarring an otherwise clean silhouette.
      FIX: Only stroke the outer silhouette of the torso mesh; weld or hide interior patch-boundary edges (or set their stroke alpha to 0) so shared seams between torso patches don't render as visible scars.

======================================================================
SEKEM  mean 4.4  min 3.5

  VERDICT afrobeats: The dwelling side-to-side weight commit is there, but the cat is doing "fists parked at the ribs, legs cross-step" — not sekem's shoulder-dig pump, and any Lagos aunty at owambe would call it out in two seconds.
  VERDICT movement: The legs shuffle and twist gamely on the beat, but the arms — the whole point of a sekem — never actually pump; they sit locked in one guard shape for all 32 frames, so this reads as generic weight-shift-with-crossed-arms, not sekem.
  VERDICT director: This is a fidgety idle stance with crossing feet, not a Sekem shoulder-dig pump — the one gesture that defines the move never happens.
  VERDICT anatomy: The signature shoulder-dig pump never actually happens — the arms sit frozen in one held pose per bar while a pair of permanently knee-locked legs and a single rigid torso block do all the work.
  VERDICT rigging: Clean, poppy-free interpolation and stable ribbons, but the rig never delivers the sekem's signature shoulder-dig pump and carries recurring torso interpenetration — competent but not sign-off-ready.
  [afrobeats] (f0) The free arm never pumps. I measured pixel variance of the low-hand region across all 8 beat hits (frames 0,4,8,12,16,20,24,28) and it is dead flat — the 'dig' arm just sits parked near the hip the whole 2-bar phrase instead of driving down past the hip on the beat. Sekem's entire identity IS that pump; without it this is just a hands-on-hips shuffle.
      FIX: Key the free paw's local y with a real beat-synced excursion (rest near lower ribs -> drive down past the hip line -> snap back) with an easeOutBack dig on the beat, not a static hold.
  [afrobeats] (f16) The pinned/pumping arm assignment never swaps sides. Frame 16 (start of bar 2) is pose-identical to frame 0 for both arms — same paw high, same paw low — where the source move calls for the roles to flip every bar.
      FIX: Mirror the paw-target assignment (which side is pinned vs. pumping) at each 16-frame bar boundary so the accent visibly alternates over the 2-bar loop.
  [afrobeats] (f0) Hand geometry is wrong vocabulary: both paws sit forward at the dancer's own sides (generic 'boxer guard' paddle), not one paw crossed to the sternum centerline with the other tucked to the small of the back — the actual MC Galaxy anchor pose named in the research doc.
      FIX: Cross the pinned hand to x≈0 at sternum height, and route the other arm's IK target behind the torso silhouette (back-waist), even if partially occluded — that occlusion is itself the tell of an authentic sekem.
  [movement] (f4) The onion-skin overlay of the full loop shows the torso/arm block barely moving at all across 32 frames while the head and legs sweep through a wide fan — the free arm never travels from tucked-at-ribs to extended past the hip. The 'shoulder dig' that defines this move (a driven downstroke with a felt arrival) is missing; what's there is a held pose, not a pump.
      FIX: Key the free-arm fist/elbow to travel a real distance — from tucked near the ribs to extended past hip level — on every odd beat, with a fast strike down and a slower, weighted recoil back up, so the arm has its own effort curve independent of the legs.
  [movement] (f16) At frame 16 (the bar 2 downbeat, where the brief calls for the pinned/pumping arms to swap sides) the arm shapes relative to the torso are identical to frame 0. The only thing that changed is the camera-facing body orientation from a turn; the audience never sees the pinned hand release and the working arm take over — the swap is inferred from staging, not performed.
      FIX: Explicitly re-key which arm is pinned at the sternum and which is pumping at the bar boundary, so the hand-off is visible on the body itself regardless of which way the character is facing.
  [movement] (f15) The ~150° reorientation that reveals the tail happens within essentially one sampled interval (frame ~15 to 16) with head, shoulders, and hips all arriving together — it reads as a viewpoint snap-cut rather than a plant-foot twist. A real twist would show the hips leading and the ribcage/arms trailing and catching up (whip and settle).
      FIX: Spread the turn across 6-8 frames with a clear lead-lag chain: hips/plant foot rotate first, ribcage follows a couple frames later, arms trail and slightly overshoot before settling — so the twist reads as one kinetic chain instead of the whole rig rotating as a single rigid block.
  [director] (f0) The 'free' fist never pumps down past the hip. Across all 24 sampled cells (all 32 frames), one paw stays pinned near the sternum/tie as intended, but the other paw just idles fisted at the belt line — it never travels down past the hip on an accented shoulder dig. The signature gesture that makes this 'Sekem' and not generic idle-shuffle simply isn't in the keyframes.
      FIX: Add a real low extreme: drive the free wrist down and slightly outside the hip line (past the pocket, arm visibly extending away from torso) on the beat, with a fast in / slow-settle timing so it reads as a dig, then return — don't let it park at rest height.
  [director] (f16) Bar 2 does not swap which arm is pinned and which pumps. Frame 16 (top of bar 2) shows the identical arm assignment as frame 0 — same hand at the sternum, same hand fisted low, same side. The brief calls for the roles to swap per bar; instead the whole 2-bar loop reads as one static arm pose held for 8 beats while only the legs shuffle, which is the main source of the monotony across the strip.
      FIX: Mirror the arm rig at the bar boundary: swap sternum-pinned hand and pumping hand left/right at frame 16, and mirror the torso/shoulder lean with it so the second bar is legibly the 'other side' of the groove, not a repeat.
  [director] (f12) The footwork reads as a walk-in-place scissor step, not a plant-foot twist with a side-to-side weight commit. Legs simply alternate crossing under a torso that stays centered and camera-facing the whole loop (see frame 12) — there's no hip rotation over a planted foot and no visible weight transfer, so the lower body never sells the 'commit to a side' feeling the move needs.
      FIX: Pick one foot as the plant per bar, rotate the pelvis/ribcage over it (torso yaw + slight lean toward the working side) while the free leg only taps, then untwist on the swap — right now the hips stay square to camera throughout.
  [anatomy] (f12) The 'pumping' arm never pumps: comparing frame 0 through frame 12 (a full bar, 4 beats), the pinned fist and the free fist are in an identical silhouette — same elbow angle, same shoulder flexion, same hand height at the hip crest. There is zero shoulder/elbow excursion anywhere in the bar; the legs and torso do all the stepping while the arms are held as a static prop, so the move's namesake shoulder-led dig is not animated at all, only implied by a fixed hand-on-hip pose.
      FIX: Key the free arm's shoulder+elbow chain on its own timeline, independent of the leg cycle: full glenohumeral extension driving the fist down past mid-thigh on the downbeat 'dig', then a quick recoil back up to the current hip-hover on the upbeat. The pinned hand should also show a small isometric flex (not travel, but visible muscular engagement) so it doesn't read as glued on.
  [anatomy] (f16) At the bar-two transition the whole trunk pivots as one inert plank: the shoulder line and the pelvis line rotate through the same angle at the same rate through the plant-foot twist, with no lag or counter-rotation between ribcage and hips. A shoulder-led groove needs the ribcage to lead the pelvis (oblique-driven spiral) to generate the torque that reads as a 'dig'; here the spine has no independent segments, so the twist looks like the whole body swiveling on a lazy-susan rather than a torso pump.
      FIX: Split the spine into at least two rotating segments (pelvis and ribcage/shoulder girdle) and offset them — let the ribcage rotation lead the pelvis by 1-2 frames and/or exceed it in amplitude, then let the pelvis catch up. Even a 10-15 degree differential will read as a real spinal counter-twist instead of a rigid block turn.
  [anatomy] (f0) Both legs are dead-straight rods hinged only at the hip in every sampled frame, including the weight-bearing/planted leg at its most loaded point (frame 0, also visible at frame 5) — there is no knee flexion anywhere in the 32-frame loop, on either the standing leg or the reaching leg. A real body absorbing a lateral weight commit loads through 10-20 degrees of knee flexion; a permanently locked knee taking lateral load like this is both mechanically stiff-looking and would be a real hyperextension/injury risk for an actual dancer.
      FIX: Add a small knee-flexion arc keyed to the beat: bend the weight-bearing knee slightly as it catches the weight commit, straighten (but don't lock) as it releases into the next step. Apply the same softening to the reaching leg's knee so neither leg is ever a perfectly straight line from hip to ankle.
  [rigging] (f0) The 'free' arm never travels past the hip for the whole 32-frame loop — it sits pinned near the chest/hip the entire cycle with only a left/right hand-swap around the midpoint. The defining 'pump down past the hip on the shoulder dig' amplitude is essentially absent, so the move reads as a held two-fist pose with a leg shuffle rather than a pumping groove.
      FIX: Re-key the elbow/wrist curve so the fist has a real vertical excursion clearing past the hipline each half-bar (verify in pixels against the silhouette, not just in rotation degrees), and check whether a shoulder/elbow ROM clamp is truncating the travel before it gets there.
  [rigging] (f16) At the underarm/elbow where the resting arm meets the torso, the ribbon mesh cuts a sharp concave V-notch straight into the jacket silhouette instead of a soft cloth fold — reads as a clipping dart, not compression, and repeats identically at every pose in this arm configuration (also visible at frames 23 and 27).
      FIX: Add an underarm gusset polygon or soften the elbow-to-torso blend radius so the ribbon meets the jacket silhouette with a convex fold instead of carving a wedge out of it.
  [rigging] (f7) The necktie balloons into a wide paddle-shaped blob as it drapes past the sternum-pinned fist instead of holding a tapered cloth width — the tie mesh appears to be stretching in-plane to dodge the fist rather than passing behind it in depth.
      FIX: Drive the tie with its own simple taper-constrained ribbon deformer and route it behind the fist in z-order/depth rather than letting the fist's collision push the tie's 2D width outward.

======================================================================
BUGA  mean 6.4  min 5.5

  VERDICT afrobeats: The one-arm-thrust problem is fixed — the hit now reads as a real bilateral peacock bow on a count-accurate three-dip sink — but the leg base underneath both phases is a runway stride, not the grounded lo-lo-lo pocket, and the promised shoulder shrug never actually shows up on camera.
  VERDICT movement: The lo-lo-lo sink is genuinely well-timed to the beat grid and builds real weight, but a clipped nadir, a shrug-less static "hit," and a carbon-copy second repeat keep it short of camera-ready.
  VERDICT director: The hit's silhouette contrast reads fine, but the choreography's actual promise — a building three-count sink and a shrug on the hit — never happens on screen; it's a single step-down-and-hold followed by a shrug-less arm-fan, looped twice unchanged.
  VERDICT anatomy: Individual joint angles stay within human range, but the torso never pays the price for how wide the stance gets, and the one beat that's supposed to read as a shoulder shrug actually drops the shoulders — plausible in stills, unconvincing as a weighted, accented dance move.
  VERDICT rigging: Clean, appealing linework and a well-held silhouette throughout, but a real foot-plant slide on the sink hold and a tail that tangents/flickers at the wrist keep this below sign-off quality.
  [afrobeats] (f8) During the sink, the trailing leg is almost fully extended on the toe while only the lead knee bends, so the 'lo-lo-lo' reads as a forward lunge/stride rather than a low, grounded double-knee bounce. A Lagos audience knows Buga sits low and centered, not stepping forward like a fencer.
      FIX: Keep both knees flexed roughly 30-40 degrees through all three sink counts and drive the dip from pelvis/rootDy only, not leg-straightening on the back leg, so the base looks like a settled bounce, not a stride.
  [afrobeats] (f12) Comparing the sink shoulder line to the hit shoulder line, they're identical — the accent is arms-only. The audit's own fix called for clavicle/socket shrug keys firing on the hit, but nothing visibly pops at the shoulders when the arms bow open.
      FIX: Add a fast 1-2 frame clavicle-up impulse timed exactly to the hit landing (shoulders jump toward the ears, easeOutBack) before settling into the held bow, so the shrug reads as a distinct beat, not just wider arms.
  [afrobeats] (f13) On the hit itself the back foot is still heel-raised/toe-off rather than planted, so the signature 'show-off present' looks like a body caught mid-run with arms flung out rather than a decisive stuck landing the audience can register and react to.
      FIX: Settle the back foot flat (or heel-down) at the instant of the hit so the pose reads as a plant, then release the foot back into motion on the following sink.
  [movement] (f8) On count 3 of the sink, the hips/torso drop lower than the leg pose supports, and the shoes get cut off the bottom of frame entirely — measured head-top position goes 112→127→154px deeper across beats 0-1-2 (a real progressive sink, good), but by the third count the character has translated so far down that both feet are clipped past the visible ground line rather than resolving into a deeper knee-bend.
      FIX: Cap the vertical root-descent on the third 'lo' (or route more of that depth into visible knee/ankle bend instead of a straight hip-translate) so both shoes stay fully inside frame at the lowest point; verify at exactly frame 8 in the film strip.
  [movement] (f12) The 'show-off present with a shoulder shrug' lands as a symmetric, static arms-out flare — no shoulder elevation toward the ears is visible (the collar/neck line is identical to the sink pose), and the pose is then held almost pixel-for-pixel identical for a beat and a half (frames 12→15) with no rebound or settle, reading as a freeze rather than a flourish.
      FIX: Layer an actual shoulder-elevation key (both shoulder points rise ~1 head-width toward the ears) into the accent, offset slightly from the arm-open so the shrug reads as its own beat of articulation, then add a small overshoot-and-settle on the arms instead of holding a dead symmetric T.
  [movement] (f16) Beats 4-7 are an exact repeat of beats 0-3 — same head-height curve (112/130/159/99 vs 112/127/154/99) and the same hands-on-hips/lean/open-arm silhouettes reused verbatim — so the second half of the 2-bar phrase adds nothing and the loop reads as a copy-pasted GIF rather than a performed build.
      FIX: Differentiate the repeat: shift weight or travel direction on the second lo-lo-lo, deepen the final sink or widen the presentation arms further on the second hit, so the phrase escalates across the full 8 counts instead of replaying identically.
  [director] (f8) The 'progressive lo-lo-lo sink over three counts' is not progressive — the rig drops once from beat 1 to beat 2 and then holds that exact same low COG through beat 3 (frame 8 is pixel-identical in torso height to frame 4). There's no third, deeper level, so the groove reads as duck-and-freeze instead of a building sink.
      FIX: Key three distinct hip-height levels, one per count (e.g. -8px / -16px / -24px on the pelvis), so count 3 is visibly lower than count 2, not a repeat of it.
  [director] (f12) The named 'shoulder shrug on the hit' doesn't exist in the render. On the hit, the shoulder line stays flat while both arms slowly fan open to the sides over a full beat — it reads as a symmetric jumping-jack reveal, not a shrug-and-present accent, and the opening has no snap (linear ease-in, no hold or overshoot on the beat).
      FIX: Add a fast trap/shoulder raise (~10-15px) that lands exactly on the beat and holds 1-2 frames before the arms settle outward, with a slight overshoot-then-settle so the accent is felt, not drifted into.
  [director] (f24) Beats 5-8 (frames 16-31, cells 18-23) are a pixel-identical repeat of beats 1-4 (frame 24 matches frame 8 exactly, frame 28 matches frame 12) — within a single 8-beat phrase the audience watches the exact same sink-and-present twice with zero variation, which reads as a looping GIF rather than a performed take.
      FIX: Vary the second repetition: swap which arm leads the open, add a head tilt or tail flick on the second sink, or push the second hit's amplitude further for a felt build across the phrase.
  [anatomy] (f20) In the wide sink/lunge poses the spine and pelvis stay dead-centered and perfectly vertical while the legs scissor out into a cossack-width stance (front knee bent, back leg locked straight). No standing body can hold its center of mass exactly between two feet that far apart without toppling toward the straight-leg side — the hips and ribcage must translate laterally over the loaded, bent-knee leg.
      FIX: Drive the spine/pelvis root joint with a lateral offset (and slight forward lean) toward the bent-knee leg, scaled to stance width, so the torso is visibly stacked over the working hip at maximum sink instead of pinned to a fixed centerline.
  [anatomy] (f12) The choreography calls for a shoulder shrug on the show-off hit, but comparing the groove pose to the presenting/hit pose shows the opposite: the neck lengthens and the shoulder line drops lower right at the moment the accent should land as scapular elevation toward the ears. The rig's resting hands-on-hips pose is already more hunched than the 'hit,' so there's no elevated position left for the shrug to pop into.
      FIX: Add a discrete trapezius-elevation keyframe (shoulders up, neck visually compressed) exactly on the hit frame that is the highest shoulder position anywhere in the phrase, then let it settle over the next beat; the groove pose should sit at a lower shoulder baseline so the shrug has somewhere to travel from.
  [anatomy] (f28) On the presenting hit, both elbows snap from a bent, tucked-at-hip position to fully locked, mirror-symmetric extension in the same instant, with identical abduction angle left and right. Real arms never leave a joint position and arrive at end-range in perfect lockstep symmetry; this reads as a rigid prop swinging open rather than a dancer's flourish.
      FIX: Stagger the two arms by 1-2 frames (lead/follow), leave a few degrees of elbow flexion short of full lock, and vary the left/right abduction angle slightly so the pose isn't a mirrored duplicate.
  [rigging] (f3) During the first sink hold, the leg/stance pose is essentially unchanged (same weight distribution, same knee bend, same foot angles) yet the whole rig translates rightward across the floor frame to frame — an overlay diff of frames 0 and 3 shows head, torso and both feet shifting by the same uniform delta. That's a textbook foot-skate: the feet are not gripping the floor, the root is just gliding under a static pose.
      FIX: Drive the hold beats from a foot-plant/IK lock: pin the grounded foot's world-space position and solve the pelvis/root offset from that anchor, rather than keying root translation independently of the leg pose. Only allow root travel on frames where the stance itself changes (weight transfer, new plant).
  [rigging] (f21) In the hands-on-hips hold, the tail's rest path threads directly behind the wrist/cuff, and at frames 19 and 21 the tail tip touches the cuff edge, converging with the sleeve hem and jacket bottom into one messy point — reads momentarily like a third limb growing out of the wrist instead of a tail behind the hip.
      FIX: Re-route the tail's control curve to clear the arm's bounding volume by a small margin (push its mid-control point back/down away from the wrist locator), or add a depth bias so it sorts fully behind the forearm ribbon with a visible gap, not a touching edge.
  [rigging] (f20) Within the same static hold, the tail is visible peeking past the hip at frame 19, fully hidden at frame 20, then visible again at frame 21 — it pops in and out of occlusion between adjacent frames instead of holding or settling smoothly, which reads as a z-sort/visibility flicker rather than secondary tail motion.
      FIX: Key the tail's depth/occlusion continuously across the hold (interpolate its sort offset and silhouette clip every frame) instead of only at the pose keyframes, so its visibility change is a smooth reveal/hide arc, not a binary pop between frames.

======================================================================
POUNCINGCAT  mean 6.1  min 5.5

  VERDICT afrobeats: Nice cat-specific touches (tucked paws, tail flicks, dead-level head) but the signature cross-body paw swipe gets swallowed by a dominant symmetric double-arm hold and the base never actually glides — needs a revision pass before it's pro-reel ready.
  VERDICT movement: The rebound accent is genuinely well-anchored to the beat, but the head breaks its own "dead-level" rule at the pose the audience actually looks at, and the compress-swipe-rebound cell doesn't repeat with even weight — competent, not yet signature.
  VERDICT director: One nice flex-pose bookend, but the loop spends most of its runtime frozen in a single parallel-arm lean, so the promised compress/cross-body-swipe/rebound contrast never actually reads on screen.
  VERDICT anatomy: Expressive face and clean weight shifts, but the legs never bend at the knee, the swipe arm blows out into an unjointed rubber limb on its accent beat, and the head bobs against the move's own dead-level-head premise.
  VERDICT rigging: Head-lock and arm-sweep volume read well, but a recurring collar/tie clip, a static "prop" tail, and a rolled front-foot contact keep this out of sign-off range.
  [afrobeats] (f0) The 'cross-body paw swipe' is meant to be one paw crossing the body while the other counter-balances, but for most of the loop (frames 0-5, 12-14, 19-23) both arms move as a mirrored pair in the same wide horizontal reach — it reads as a generic two-arm dance hold, not a cat pawing at something.
      FIX: Break the mirror: keep the trailing arm's fist tucked tight at the ribs/hip for the whole phrase and let only the lead paw travel across the body, so the eye reads ONE limb acting, with the other as still counterweight.
  [afrobeats] (f12) The base is described as 'gliding' but the crossed-lunge foot silhouette is essentially frozen in place across nearly the whole 32-frame loop — compare frame 0 and frame 12, which land in almost the same foot position with no lateral travel — so there is no visible glide under the still head, just a held static stance.
      FIX: Add a small continuous lateral hip/foot drift (a couple of pixels of travel per bar, shuffle-step style) so the lower body visibly slides while the head stays locked level — that contrast is the whole point of the Amapiano pairing and right now only the head half of it is delivered.
  [afrobeats] (f6) The named three-part shape (compress, swipe, rebound) collapses into a two-pose pop: between frame 5 and frame 7 the arm jumps from a fully extended wide reach straight to a paw-at-chin/leg-kick with no visible coiled compress in between, so the swipe reads as a snap rather than a phrased build.
      FIX: Insert a genuine held compress frame (both paws drawn to the chest, shoulders rounded, weight sunk) for a full beat before the swipe fires, so compress/swipe/rebound are each legible as their own beat instead of implied by the two extremes.
  [movement] (f24) The move's whole identity is a dead-level head over a moving body, but at the rebound apex — the most held, most-looked-at pose in the phrase, landing square on the beat — the head tilts with the torso lean instead of staying screen-level.
      FIX: Lock head roll to 0° through the rebound (counter-rotate the neck against torso lean) so only the body's line changes; the head should read as a fixed anchor the shoulders swivel under, not another limb that leans.
  [movement] (f12) The compress hold is a different length each time it recurs: at frame 12 it's touched for roughly one beat before diving straight back into the swipe, while the same pose at the loop seam (frames 28 through 0) is held for a full two beats. Same choreographic unit, two different weights — reads as a timing slip rather than a repeatable motif.
      FIX: Normalize the compress dwell to a fixed count everywhere it appears (pick one beat, or two, but not both), or if the longer hold at the seam is a deliberate phrase-end mark, give it a visible extra accent (deeper sink, breath) so it reads as intentional rather than inconsistent.
  [movement] (f1) The 'cross-body paw swipe' snaps from tucked-at-hip to full extension within a single sampled frame (about a 16th note) and then sits almost static through the next two samples — the arm arrives dead rather than traveling through the cross-body path, so the swipe reads as a pose-to-pose snap, not a swept effort action.
      FIX: Spread the extension across the full beat with an eased arc — wrist leading, elbow trailing, shoulder rotating open — and keep the arm drifting slightly through the hold instead of freezing, so the travel itself is visible before the rebound wind-up begins.
  [director] (f9) The named 'cross-body paw swipe' never crosses the body — both forearms swing to the same side and stack in parallel (visible at frame 9, and it recurs almost pixel-for-pixel at frame 17), so at a glance it reads as a static lean/shrug rather than a swiping claw gesture.
      FIX: Drive the lead arm through a real diagonal arc from the opposite shoulder toward the opposite hip while the trailing arm anchors close to the ribcage, so the two limbs form a visible X/diagonal across the torso instead of two parallel lines pointing the same way.
  [director] (f12) Compress and rebound don't contrast in height or silhouette — the torso sits at nearly the same medium-crouch level through the compress beat, the swipe, and most of the rebound, so the Amapiano weighted-compress-vs-light-rebound idea is invisible at a glance.
      FIX: Drop the hips/chest visibly lower on the compress beat and let the rebound apex (around frame 24) actually extend the spine taller/lighter than the swipe frames, so the loop reads three distinct silhouette heights instead of one held shape.
  [director] (f28) The loop hitches into a dead hold right before it resets: frames 28, 29 and 30 show an almost identical 'hands on hips' pose with no perceptible change across three consecutive samples — a full beat is essentially freeze-framed exactly at the seam where a viewer's eye is most likely to catch a stutter on loop.
      FIX: Keep a small continuous motion running through that hold (weight rock, tail flick, micro head turn) or cut the hold shorter and give those frames back to the swipe/rebound arc so energy doesn't flatline before the phrase repeats.
  [anatomy] (f0) Both legs are single straight tapered segments from hip to ankle in every stance frame, including the deepest 'compress' pose — there is no visible knee joint anywhere in the loop.
      FIX: Add 10-20 degrees of knee flexion on the loaded/front leg during the compress beats (relaxing toward straight only at the rebound), so the lunge reads as a coiled joint chain instead of two hip-hinged stilts.
  [anatomy] (f7) In the cross-body paw swipe, the swiping arm renders as one unbroken straight ribbon from shoulder to fist with no elbow bend, and it reaches noticeably farther from the body than the guard arm or the character's own thigh — it reads as a stretched rubber limb, not a jointed swipe. The same beat position later in the loop (~frame 23) shows the correct compact, elbow-bent version, so this is an inconsistent outlier, not a deliberate exaggeration.
      FIX: Insert a mid-limb elbow-bend keyframe on the swipe arm and clamp the ribbon mesh to the rig's shoulder-to-wrist rest length so it can't stretch past anatomical reach; match this pose to its correctly-bent repeat later in the loop.
  [anatomy] (f8) Measured ear-tip height swings by roughly a quarter of the character's on-screen height between the compressed base pose (head low) and the swipe extension one beat later (head high) — the head is riding the torso's vertical travel rather than staying level, which directly undercuts the move's stated 'dead-level head over a gliding base' contrast.
      FIX: Decouple head/neck vertical translation from the pelvis bob with a counter-translation in the neck IK so head world-space height stays within a small band while the legs do all the visible up-down work.
  [rigging] (f8) On every cross-body swipe beat, the crossing forearm's fist lands directly on the popped collar and tie knot, cutting the tie with a hard notch instead of a clean occlusion edge — reads as mesh interpenetration at the neck, not a paw passing in front of the chest.
      FIX: Give the collar/tie a neck-locked layer that always draws above the sleeve within a small radius around the throat, or shrink the fist's placement/collision so it stops short of the collar footprint at full cross.
  [rigging] (f27) The tail always terminates at the same relative point against the trailing fist/hip regardless of pose, and its silhouette runs tangent and parallel to the near leg for a stretch — it reads as a static decorative shape glued to the hip rather than a deforming ribbon reacting to the body.
      FIX: Drive the tail tip with its own lag/spring off the hip socket so curl and length vary with arm/torso pose, and push its base attachment behind the leg in z-order to break the tangent.
  [rigging] (f4) At full compression the leading foot rolls onto its edge (sole turned near-vertical toward camera) instead of sitting flat on the ground plane, and the ground shadow doesn't match the rolled sole orientation — an ankle-inversion look that undercuts foot-contact stability.
      FIX: Clamp ankle roll against the ground-contact IK at the max-compress keys so the sole stays flat, and re-project the shadow ellipse from the actual sole contact edge rather than a fixed offset.
