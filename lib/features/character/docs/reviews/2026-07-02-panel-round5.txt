
======================================================================
SHAKU  mean 6.4  min 5.5

  VERDICT afrobeats: The crossed-wrist X is finally the landlord, not the visitor — the vocabulary fix from the audit clearly landed — but a dead face and a generic "elbow pump" instead of a real scoop keep this from being a Lagos-ready signature.
  VERDICT movement: A musically smart, well-timed shaku with real gallop footwork and clean beat-4/beat-8 accents, but the signature scoop loses its low, sweeping shape and the torso rides passively above legs that are doing all the dancing.
  VERDICT director: The stagger-step skeleton and phrase structure are right, but the signature X is swallowed by the torso, the one accent beat barely differs from the hold, and the promised off-axis lean never shows up — it plays as a stiff, repetitive placeholder rather than a camera-ready Shaku Shaku.
  VERDICT anatomy: The crossed-wrist X and akimbo/reach transients read cleanly, but a recurring broken-ankle foot silhouette and a torso that never leans or transfers weight into the stagger keep this below professional-reel standard.
  VERDICT rigging: Stance, weight shift, and loop continuity are solid, but the move's signature crossed-wrist X reads as a fused blob and one transition arm collapses into a joint-less paddle — not sign-off clean.
  [afrobeats] (f13) The transient release out of the X reads as a symmetric elbows-back/fists-at-hip pump (like a sprinter's arm drive), not the outward, slightly downward scoop that gives shaku its name and its visual punctuation against the held cross.
      FIX: On the open key, drive both hands outward AND down (wider x, lower y than the cross, palms trailing) with a quick asymmetric snap rather than a mirrored guard pose, so even a single glimpsed frame reads as a scoop, not a boxer's ready stance.
  [afrobeats] (f0) The held X posture is carried almost perfectly upright/vertical at the top of the phrase (frame 0) instead of the off-axis lean the brief calls for as part of the base posture; the lean only shows up faintly a few beats later (e.g. frame 18), so it reads as incidental wobble rather than a deliberate carriage.
      FIX: Bake a sustained forward/off-axis chest tilt (5-8 degrees) into the held-cross keys themselves, not just the mid-phrase dip keys, so the lean is present every time the X is held, not just intermittently.
  [afrobeats] (f8) The face/head stays camera-locked with the same neutral open smile through every held-X frame (0, 8, 16, 24 all identical) — there is no chin dip, head cock, or smirk to sell the 'handcuffed' attitude that Lagos audiences read instantly on this move.
      FIX: Add a small head tilt/chin-down on the held-cross beats reusing the shoulder-isolation channel already built for buga, so the head participates in the pose instead of floating neutral above it.
  [movement] (f12) The 'open scoop' at the beat-4 accent lands as a static hands-on-hips flex at belt height instead of the wide, low sweeping scoop the shaku is named for — it reads as 'arms apart' rather than 'scooping', so the signature gesture's own effort quality (sudden, wide, low) doesn't land.
      FIX: Push the open-hand IK targets below hip line toward knee height and route through an intermediate wide/low arc frame before the hold, so the accent visibly scoops through space instead of popping straight to a hip-level flex.
  [movement] (f16) Through the long held-X stretches (e.g. the whole bar-2 restart) the chest/shoulder line barely moves while the legs do a full catch-step gallop underneath — the upper body reads like a fixed mount riding on independently animated legs rather than one weighted organism.
      FIX: Let the ribcage visibly counter-lean opposite the reaching leg on each catch-step beat (a few more degrees of chest roll timed to the leg plant), not just the pelvis, so the torso participates in the gallop's weight transfer.
  [movement] (f28) The bar-2 punctuation swaps to an asymmetric one-arm reach-back ('generator pull') instead of a second scoop — a legitimate variation in principle, but it changes the gesture's shape entirely, so on repeat the audience doesn't get a recognizable reprise of the shaku accent, weakening the move's signature legibility.
      FIX: Keep the same low two-hand scoop silhouette for the bar-2 accent (mirror it or scale it bigger/faster for variety) rather than substituting an unrelated single-arm pull, so 'the scoop' stays visually identifiable both times it punctuates.
  [director] (f0) The move's namesake gesture — the crossed-wrist X — is pulled in so tight against the sternum/collar that it reads as a self-hug, not a bold cross-body slice; at small-screen scale the 'X' silhouette is invisible against the torso.
      FIX: Push the forearm cross outward: raise elbows out and slightly up so the wrists cross a few inches in front of the chest, clear of the lapel line, giving the pose a diagonal silhouette break instead of hiding it inside the body outline.
  [director] (f12) The one scripted departure from the X-hold ('open scoop') is just the fists dropping to the hips with elbows tucked in — it barely differs from the base hold in silhouette, so the loop's only accent beat fails to register and the whole 32-frame phrase reads as one repeated held pose broken by leg-only stepping.
      FIX: Turn the punctuation into an actual wide scoop: sweep both arms out and down/up in an arc away from the body on that beat so it's a clearly contrasting open shape against the tucked X, not a small hand-position shuffle.
  [director] (f4) Vocabulary calls for an off-axis lean, but the spine stays dead vertical through the entire phrase — only the legs stagger side to side under a static, upright torso, which flattens the energy and reads as stiff rather than a confident Afrobeats lean.
      FIX: Drive a 10-15 degree lateral torso lean off the spine chain, alternating away from the loaded hip on each stagger step, so the upper body visibly participates in the weight shift instead of riding as a rigid block over the legs.
  [anatomy] (f17) The weight-bearing/grounded foot renders with a doubled, disconnected shoe silhouette — a red heel-cap block plus a second, separate red-and-white toe blob sitting behind/below it at an unrelated angle, as if the ankle folded back on itself. This isn't a stylization of plantarflexion, it reads as a snapped or telescoped ankle, and it's not confined to the extreme lunge: it's visible on the standing foot in the very first held pose of the loop too.
      FIX: Rebuild the foot/ankle terminus of the leg ribbon as one continuous shoe mesh with a single heel-toe silhouette per foot, and cap ankle rotation to real plantarflexion/dorsiflexion range (roughly -50 to +20 degrees) so the sole never presents two disjoint blobs from a side view.
  [anatomy] (f7) At the moments the crossed-wrist hold resets, the two forearms/fists don't cross — they melt into one indistinct fused mitt at the sternum with no visible wrist separation or crossing lines, killing the readability of the move's namesake X shape exactly when it should be clearest.
      FIX: Keep a minimum forearm-to-forearm offset (z-depth and lateral gap) through the whole hold so the crossing X and both cuffs stay legible at every frame, not just the extremes of the interpolation; add a mid-hold key that re-asserts the X silhouette.
  [anatomy] (f0) Despite the brief calling for a staggered gallop step with off-axis lean, the spine stays bolt-upright and the pelvis stays laterally centered between the two feet in every sampled frame — the legs scissor symmetrically left/right while the torso floats independent of which foot is bearing weight, so there's no visible weight transfer or counter-lean driving the stagger.
      FIX: Drive pelvis lateral translation and a few degrees of spine/rib-cage counter-lean off the stance-leg's ground contact so the torso visibly loads over whichever foot is down, then let the off-axis lean amplify on the transient open-scoop beats.
  [rigging] (f0) The held crossed-wrist X — the pose that literally defines 'shaku shaku' — doesn't read as two overlapping forearms. The fists, tie, and collar lapels merge into one amorphous orange/white clump, and the 'crossed wrist' cue is just a couple of thin disconnected line scribbles floating under the fist mass rather than a legible over/under wrist crossing.
      FIX: Give the top arm a clean, unbroken silhouette that fully occludes the bottom arm at the crossing point (solid cuff edge, no gap), and pull the fist cluster down/away from the collar so the tie and lapel points aren't competing with the wrists in the same few pixels.
  [rigging] (f4) At every crossed-arm frame, the shoulder ribbon meets the torso with a sharp concave notch/cusp right under the deltoid cap on both sides — a hard inward dart rather than a smooth taper, reading as a mesh-weighting glitch rather than a natural cloth fold.
      FIX: Add an intermediate weighted vertex at the shoulder-ribbon root so the deltoid-to-bicep taper blends continuously instead of pinching to a point, then re-check the silhouette in isolation (limb only, no torso behind it) to confirm the cusp is gone.
  [rigging] (f29) During the open/flair transition the leading (screen-right) arm loses its elbow entirely: the ribbon flattens into a single rigid wedge running straight from shoulder to fist, silhouetted like a flipper instead of a bent arm reaching to the hip/side.
      FIX: Enforce a minimum elbow-bend curvature floor on the ribbon spline during near-full extension so the forearm segment can't flatten past a set angle, then re-render this beat in isolation to confirm the joint stays readable at full reach.

======================================================================
ZANKU  mean 5.9  min 5

  VERDICT afrobeats: The stamp cadence and rib-guard fists are legit Zanku DNA, but the gbese kick that gives the move its name barely lifts off the floor and the upper body stays dead — right skeleton, missing signature.
  VERDICT movement: On-beat and structurally sound, but the legwork — the one thing this move is supposed to sell — never reads: it's a tiny, near-identical shuffle repeated almost verbatim for six of eight beats, so the body dances as a static torso holding a guard while the feet twitch underneath it, not as one organism telling a footwork story.
  VERDICT director: This reads as a static hands-on-hips hip-rock with a token knee-cross, not zanku legwork — the same 2-beat pose repeats four times across the loop with no footwork story and no build.
  VERDICT anatomy: Weight-bearing hips/knees/ankles read as plausible, but the arm silhouette repeatedly loses its elbow, the spine moves as one rigid plank, and the "tap-dig-scrape-stomp" cadence never actually shows a scrape.
  VERDICT rigging: The on-beat "hands-on-hips" contact poses are clean and stable, but the recurring off-beat leg-lift passing pose collapses into a foot/arm pileup nearly every time it appears, undercutting the very legwork clarity this move is supposed to sell.
  [afrobeats] (f21) The namesake air-kick — the ONE move a Lagos audience is watching for — only shows up for a single instant in the whole 2-bar phrase and tops out around shin height with almost no backward lean to sell it. It reads as a slightly bigger step, not the flung leg that gives Zanku its name.
      FIX: Raise the kicking foot to at least thigh/hip height with a hard knee-snap extension, add a 10-15deg backward torso lean that peaks exactly on the kick, and hold the extended pose for 2 extra frames so it registers as a punctuation, not a blur.
  [afrobeats] (f8) Arms are frozen fists-on-hips for nearly the entire loop; the only arm movement is a soft cross-body sweep that happens twice in 8 beats, not a pump on the stamps. The brief says arms should punctuate the legwork, but here they're just a held silhouette while the legs do everything.
      FIX: Keep the elbows-out fists-on-hips base pose, but drive a sharp outward/upward elbow pump on every stamp (2-3 in of travel, snap out then back), so the arms visibly answer each foot hit instead of only twitching once every two beats.
  [afrobeats] (f0) The face and head are camera-locked in a static open-mouth/smile the entire phrase — no chin-down, no glance at the working foot, no shoulder roll. Zanku's attitude is half the sell (the 'I don't care, watch my legs' look); a flat, upright, forward-facing head reads as generic idle, not Zanku swagger.
      FIX: Add a ~15deg chin-down tilt with the gaze breaking toward the tapping foot during the legwork bars, and layer in an alternating shoulder roll synced to the stamp pattern instead of the current locked shoulder line.
  [movement] (f16) The core 2-beat step is copy-pasted almost pixel-for-pixel across beats 1, 3 and 5 (and their beat-2/4/6 partners) — same wide stance, same tiny heel-toe knock, same hands-on-hips hold — so three-quarters of the phrase is a visual loop rather than a developing legwork phrase.
      FIX: Vary the footwork pattern itself between repetitions (change scrape direction, add a double-tap on one repeat, vary stance width/COM drop) so a viewer scanning the strip sees the phrase build toward the gbese payoff instead of idling until it.
  [movement] (f2) The heel-toe tap-dig-scrape 'knock' that is supposed to be the signature action only travels a few pixels and keeps the feet clustered near the body centerline, so at performance scale it reads as a nervous shuffle, not a legible dig/scrape/stomp cadence — the wide static a-frame stance dominates the read instead.
      FIX: Push the lateral foot travel and heel/toe rotation amplitude well past the current ~30px swing — the gesture has to clear the character's own stance width to register as footwork rather than idle weight-shift, the same pixel-amplitude bar already applied to the arm work.
  [movement] (f26) The one real accent of the phrase — the gbese knee-flick with the fist 'fling' at its apex — is already relaxing back into the generic rib-guard hands-on-hip pose one sampled instant later, and the arms never commit to a distinct shape at the leg's peak, so the single moment meant to punctuate the whole move dies instead of landing.
      FIX: Hold the gbese apex (leg height + flung arms) for 2-3 held frames with a clear settle/rebound before recovering, and give the arms a shape at that instant that's visually distinct from the default hands-on-hips guard so eye and story land on the same beat.
  [director] (f0) The vocabulary promises a heel-toe tap-dig-scrape-stomp cadence, but the feet are essentially static in a wide planted stance for the entire 32-frame loop; there is no visible heel-lift, toe-flex, or scrape contact anywhere in the cycle.
      FIX: Actually animate discrete foot contacts: alternate a heel-tap and a toe-dig each beat with a brief scrape-drag before the stomp, so the ankle/foot silhouette visibly changes shape every beat instead of the legs holding one static wide V the whole phrase.
  [director] (f3) The only moment where a leg leaves its static plant (the crossed-leg beat) is simultaneously covered by an arm sweeping across the torso, so the one pose meant to carry the 'legwork is the story' idea is the least readable silhouette in the whole loop.
      FIX: Stagger the timing so the arm sweep and the leg lift never peak on the same frame; keep the lifted/digging leg clear of the torso outline (kick it out to the side rather than crossing the centerline) so it reads as a distinct graphic shape.
  [director] (f16) Rows of the grid are near-duplicates of each other (e.g. frame 0 and frame 16 are essentially the same pose), meaning the 8-beat phrase is really one 2-beat idea looped four times with no variation or escalation.
      FIX: Differentiate each 2-beat repeat — vary which foot leads, widen or intensify the stance on later reps, and raise the arm-pump height/accent on the phrase's back half — so the loop plays as one built musical sentence instead of a copy-pasted cell.
  [anatomy] (f5) On the punctuating arm-pump, the sleeve renders as a single flat, tapering wedge from shoulder to cuff with no visible elbow break and the hand essentially absorbed into the mass — there is no forearm bone to read, just a paddle. A real limb cannot balloon into a flat fin like this; it reads as the elbow having dissolved rather than flexed.
      FIX: Force a visible angle discontinuity at the elbow vertex (distinct upper-arm and forearm silhouette widths) and keep the fist/cuff as a separate rounded shape at the terminus instead of letting the ribbon mesh taper continuously into it — this is the same weighted-mesh sleeve issue noted after the last ribbon-sleeve pass and it is still showing on the pump extension specifically.
  [anatomy] (f3) During the deep weight-shift/passe (torso leaning hard over the stance leg while the other knee lifts), the shoulder line stays perfectly square to camera while the whole trunk pivots as one rigid block from the hip — there is no ribcage-vs-pelvis dissociation. Real Afrobeats legwork isolates the ribcage from the pelvis so the upper body stays composed while the legs do the intricate work; here the torso just tips over like a plank hinged at the groin.
      FIX: Add a small independent thoracic counter-rotation/counter-tilt driven opposite to the pelvis tilt (even 5-8 degrees) so the ribcage looks stabilized while the hips and legs carry the accent — this also better serves the brief that 'the legwork is the story, arms punctuate,' since a stiller torso reads as more controlled.
  [anatomy] (f1) The named cadence is heel-toe tap-dig-scrape-stomp, but the ankle only ever shows two discrete states — a fully dorsiflexed heel-dig reach or a flat plant — with no intermediate frame of sustained sole/toe contact sliding across the floor. Between the flat stance and the full heel-dig extension the foot snaps rather than drags, so the 'scrape' phase the move is named for never actually happens mechanically.
      FIX: Insert a transitional contact pose where the foot is flat-ish with weight rolling from ball to heel (or heel to toe) while still sliding, before hitting the full extended dig — even 2-3 extra frames of continuous ground contact would sell the scrape instead of two poses being interpolated as a lift-and-place.
  [rigging] (f3) At the passing/tuck pose the lifted foot and the standing foot render as one overlapping blob of soles instead of a clear heel-lift over/behind the planted foot — the two shoe shapes interpenetrate directly rather than staggering in depth or lateral offset.
      FIX: Give the lifted foot explicit horizontal clearance from the planted foot's contact point (increase the IK target offset) and push it slightly forward/back in draw order with a small scale-down so the silhouettes read as two distinct shoes even when close together.
  [rigging] (f11) In the same tucked pose, the trailing arm's ribbon sleeve/cuff sinks into the hip silhouette, fusing sleeve and torso into one soft lump and creating a tangent with the tail that reads as an extra floppy shape rather than a separate limb.
      FIX: Add a minimum clearance constraint between the lower-arm ribbon control points and the torso/hip collision hull during this pose, and nudge the tail's rest path so it doesn't pass directly behind the cuff at the same depth.
  [rigging] (fNone) The identical bent-knee tuck silhouette is reused near-verbatim at multiple non-adjacent phase points in the loop (~frames 3, 11, 19, 27), and the onion-skin overlay shows the foot-contact ghosts bunched into one tight cluster rather than sweeping through distinct heel/toe/dig/scrape/stomp footprints — the rig is only producing one usable 'lift' shape for what should be a varied legwork vocabulary.
      FIX: Author at least 2-3 distinct foot-contact silhouettes (heel-dig, toe-scrape, flat stomp) with different ankle angle and ground-offset per occurrence instead of repeating one lift key, so the onion trail shows a spread footprint pattern instead of a single overlapping cluster.

======================================================================
AZONTO  mean 4.4  min 3

  VERDICT afrobeats: Azonto in name only — the mimed-action vocabulary that IS the dance never shows up; it's one static chest-hugged fist pose stepped through both bars, so no Lagos or Accra eye would call this azonto.
  VERDICT movement: The step-touch groove and beat-timed kicks are solid, but the entire pantomime identity of azonto is missing on screen — arms sit locked in one crossed guard for all 32 frames, so neither the wheel-steer nor the cross-body jabs the choreography is named for ever appear.
  VERDICT director: The arms lock into one crossed-guard shape for the entire two bars while only the legs shuffle, so neither the wheel mime nor the jab/chamber ever actually happens.
  VERDICT anatomy: The step-touch leg mechanics are clean and safe, but the shoulders/elbows/wrists are locked into one isometric crossed-fist grip for all 32 frames, so the move never actually mimes a wheel or throws a jab — the named vocabulary isn't physically present in the joints.
  VERDICT rigging: Ribbons hold their volume and feet stay planted through the loop, but a recurring tie/hand depth-sort glitch and a faceted sleeve pinch at the crossed-arm hold are visible in the lit render and keep this below sign-off quality.
  [afrobeats] (f4) Bar 1 is supposed to mime steering a car, but the hands never leave a tight fist-to-fist cluster against the sternum — no shoulder-width grip shape, no circular turning, no downward-pointing hand carriage. It reads as a boxer's guard, not a wheel.
      FIX: Widen the hand targets to roughly shoulder width at belt/sternum height with a loose gripping hand shape, and drive an alternating up-down (10 o'clock/2 o'clock) rotation across the 4 frames of each beat so the turning motion is legible in silhouette.
  [afrobeats] (f20) Bar 2 is supposed to alternate a cross-body jab with the idle paw chambered at the hip, but the pose is visually identical to bar 1 — arms stay clamped near the chest the whole bar, with no arm ever extending outward and no paw ever dropping to rest at the hip.
      FIX: On the jab beats, extend the lead hand across the body to opposite-cheek height (a real reach, not a forearm shrug) while the off hand's fist drops and holds at the hip bone for the full off-beat, then swap sides next bar-half so the on/off contrast is unmistakable.
  [afrobeats] (f0) The head/face never turns or reacts across all 32 frames (same forward-facing wide-eyed look in every cell), so the witty inside-joke attitude that sells azonto's mimes is absent — this compounds the arm problem since there's no secondary read to compensate.
      FIX: Add a beat-synced head turn (roughly 15-20 degrees toward the working arm) plus a small smirk/eye-narrow at the contact point of each mime, matching the doc's head-turn-plus-smirk anchor.
  [movement] (f0) Bar 1's 'steering wheel' mime never reaches the screen: the arms stay locked in a single crossed guard at sternum height across every one of the 24 sampled cells, though the source keys (_azontoHandLTargetKeys/_azontoHandRTargetKeys) call for the grips to counter-rotate from brow height (y:-82) down to rib height (y:-38) over these first two beats.
      FIX: Confirm the azonto hand IK-target channels are actually driving the sleeve end-effectors post ribbon/weighted-mesh rework (character_scene.dart / cat_in_suit.dart are both mid-edit) rather than being clamped back to a default crossed-guard pose; re-render only after the torso-space y:-82 to y:-38 excursion is visibly reaching the rig.
  [movement] (f16) Bar 2's alternating cross-body jab with idle-paw-at-hip is entirely absent: the code stages a full extension past the far shoulder line (x:32, y:-54) answered by a chambered paw at the hip crest (y:-10), but frame 16 onward shows the identical static crossed pose as bar 1 — no jab, no chamber, no side-alternation.
      FIX: Verify the same IK wiring for the R/L hand channels at the frame-16 boundary; once live, check against the code's own caution that a half-reach target folds the elbow into a 'stump' below full extension, so the jab actually clears the opposite shoulder line.
  [movement] (f12) With both hand mimes inert, the only expressive material left is the legs (step-touch groove and the knee-lift kick around this frame); the upper body reads as dead weight riding on top of working legs rather than one organism, and no accent contrast survives anywhere above the waist.
      FIX: Until the hand-target channels are confirmed live, add even a placeholder shoulder/torso counter-rotation synced to the leg accents so the top half isn't visibly frozen while the bottom half dances; long-term this becomes moot once the wheel/jab arm motion is actually rendering.
  [director] (f0) Bar 1 is supposed to mime steering a wheel, but the paws stay clenched together at sternum height the whole time instead of spreading to wheel-width and rotating hand-over-hand.
      FIX: Widen the hand placement to roughly shoulder width and key an alternating over-under hand rotation on beats 1-4 so the grip-and-turn reads in silhouette.
  [director] (f16) Bar 2's cross-body jab / hip-chambered idle paw never appears — the torso and arms at the bar-2 downbeat are essentially identical to bar 1's crossed pose, just with a different leg underneath.
      FIX: Author a distinct bar-2 arm key: one paw punches fully across the body on the jab beat while the other retracts to sit chambered at the hip, alternating sides beat to beat, so it visually contrasts with bar 1.
  [director] (f20) Because the upper body never changes, cell 3/6/9 (bar 1) and cell 15/18/21 (bar 2) are near-duplicate poses — the two-bar phrase reads as one bar looped twice, killing variety and the poster-frame value.
      FIX: Give bar 2 its own distinct silhouette (extended jab arm, open stance) rather than reusing bar 1's crossed-arm key, so scrubbing through the loop shows two clearly different beats, not a repeat.
  [anatomy] (f16) Bar 2 is supposed to be alternating cross-body jabs (frame ~16 onward), but the shoulder/elbow/wrist configuration is pixel-for-pixel the same isometric X-cross held since frame 0 — no shoulder flexion, no elbow extension, no alternation ever occurs.
      FIX: Key an actual jab: shoulder flexion to ~140-160°, elbow extension to near-full, wrist neutral, thrown across the midline on the beat and retracted to a chambered elbow before the next hit, alternating sides bar-to-bar.
  [anatomy] (f13) Around frame 13 the same locked fists briefly yaw to face camera (a passing hint of a wheel grip) then snap back to the side-profile cross on the very next sampled frame with no held pose or easing — reads as a rig twitch, not a mimed gesture.
      FIX: If this is the intended steering-wheel beat, commit to it: hold a genuine bilateral forearm pronation/supination with a shoulder rock left-right across at least one full beat, eased in and out, rather than a one-frame incidental angle change.
  [anatomy] (f3) At maximum torso lean the entire shoulder girdle (both crossed forearms plus ribcage) translates as one undeformed rigid block riding atop the spine while only the pelvis/legs counter-lean — there's no scapular give or wrist micro-adjustment reacting to the trunk's lateral flexion, so the arms read as strapped to the sternum rather than actively held by a person.
      FIX: Decouple the shoulder-girdle read from pelvis rotation with a small independent counter-rotation (~10-15°) and let the wrist-crossing point re-settle slightly each beat instead of staying pixel-locked to the sternum through the full lean.
  [rigging] (f23) Tie geometry z-fights with the chambered/crossing fist: the tie knot renders as a stubby, rounded blob fused into the orange fist instead of reading as a tie passing behind or in front of the hand, and a thin black outline stroke (from the lapel/collar) cuts across the fist as a stray crack in the silhouette.
      FIX: Fix draw order so the tie mesh is always fully behind the hand/forearm ribbon at the chest (or fully in front, but consistently), and make sure lapel/collar outline strokes are clipped to their own layer so they stop compositing on top of the hand mesh.
  [rigging] (f1) At the held crossed-arm 'steering wheel' pose, the near forearm's ribbon pinches to a hard, faceted diagonal edge at the elbow crossing instead of tapering smoothly like a cylindrical sleeve, leaving a thin wedge of jacket-navy visible through the crossing.
      FIX: Add ribbon width clamping (minimum cross-section) at high-curvature crossing angles and increase segment/normal smoothing at the elbow joint so the sleeve silhouette stays convex instead of collapsing to a point.
  [rigging] (f16) The same hard faceted seam reappears on the extended cross-body jab arm where forearm meets upper arm, showing this is a systemic ribbon-crossing-angle artifact rather than a one-off pose glitch.
      FIX: Audit the ribbon deformer's joint-angle range: interpolate the cross-section normal continuously across the elbow bend rather than switching between two flat quads, so all crossing angles (not just this one) stay smooth.

======================================================================
SEKEM  mean 4.7  min 3

  VERDICT afrobeats: The pin-and-swap anchor is finally right, but the pump has no reach and the feet lunge instead of twist, so from the floor this reads as generic afro-bounce, not Sekem.
  VERDICT movement: This is a frontal, double-fist idle with a crossing-foot shuffle underneath it — the actual sekem signature, a driven shoulder-dig pump past the hip, never happens.
  VERDICT director: A tidy hands-on-belly idle with a light leg shuffle, not the sternum-pinned shoulder-dig pump the sekem demands.
  VERDICT anatomy: The torso and arms are frozen in one static hands-at-sternum pose for the full 32-frame loop while straight, locked-knee legs skate side to side — the shoulder-dig pump and plant-foot twist that define "sekem" never actually occur.
  VERDICT rigging: The weighted-mesh sleeves hold their shape well overall, but a recurring seam crack and elbow cusp spikes break the silhouette on both mirrored arm poses — needs a cleanup pass before sign-off.
  [afrobeats] (f8) The free arm's 'punch past the hip' — the entire identity of Sekem — is animated with a target excursion of only ~4px in x and ~16px in y (x stays 36-40, y -2 to 14 per the hand-target keys), so on screen the 'pump' never leaves the torso's silhouette. Sampling the beat frames back to back (0, 4, 8, 12) shows near-identical hand placement; the shoulder dig reads as a static hand resting on the hip, not a strike.
      FIX: Give the free hand a real reach: push the punch key well past the belt line (e.g. x ~60-70, y ~35-45) so the fist visibly clears the hip and the torso silhouette on the dig frame, then recoil hard back toward the ribs — the excursion needs to be legible at arm's length on a phone screen, not just present in the data.
  [afrobeats] (f4) The legs dominate the read with a wide traveling scissor lunge (front foot far forward, back leg trailing at a steep diagonal) that looks like a power-walk stomp, not the compact, grounded, shoulder-led pocket groove Sekem calls for. Because the stride is the biggest moving shape on screen every frame, the eye locks onto the legs instead of the shoulders/arms, inverting the move's priority.
      FIX: Narrow the foot IK spread substantially and keep the weight-commit as a lateral hip/knee bend under the torso rather than a fore-aft step; the visible travel should be in the shoulders and hips, with the feet doing small in-place adjustments.
  [afrobeats] (f12) No plant-foot twist is visible anywhere in the loop — both shoes stay flat with toes locked in the same forward-right orientation through every plant (frames 0, 4, 8, 12 all show identical foot angles), confirmed by cropping the foot region across beats. The audit's own non-negotiable anchor list calls out 'foot twist' and it simply isn't there.
      FIX: Add an ankle/foot rotation key during ground contact (roughly 15-20 degrees, heel pivoting in as the shoulder digs) so the plant reads as a twisting weight commit instead of a stomp-and-hold.
  [movement] (f4) The 'free' arm never leaves chest/waist height for the entire 32-frame loop — no downward excursion past the hip line anywhere in the phrase, so the pump that defines this move is simply absent; both paws sit locked in a static double-fist shape while only the legs move.
      FIX: Author real vertical wrist travel for the free arm: drive it from sternum height down below the hip silhouette on the beat (measure the excursion in pixels against the render, per the pixel-amplitude lesson), with a fast attack into the low point and a slower recoil back up, driven from the shoulder/scapula so the ribcage tips with it, not just the wrist sliding a few px.
  [movement] (f16) The side-swap that should happen 'per bar' is an instant pop: cell 11 (frame ~15) still shows the bar-1 hand arrangement and cell 12 (frame 16) is already the fully mirrored bar-2 pose with no visible transfer of weight or arm travel in between — a dead, unmotivated cut rather than a phrased handoff.
      FIX: Give the swap 4-6 frames of driven cross-body motion: let the pinned hand release from the sternum and the working arm's last downbeat carry diagonally across the chest into the new pinned position, with the hips already rotating into the new side before the arms arrive, so bar 2 reads as launched by bar 1's last accent instead of teleporting.
  [movement] (f8) The character stays camera-frontal for all 24 sampled cells; the 'plant-foot twist' is reduced to alternating which foot crosses in front, with no hip/torso rotation, so the lower body reads as marking time rather than committing weight and twisting off a planted foot.
      FIX: Rotate the pelvis/torso 15-20 degrees toward the working side as the arm digs down, holding torque through the planted leg, then untwist on the recoil into the opposite side — tie this rotation to the same beat-timed drive as the arm pump so hips, ribs, and arm read as one continuous impulse instead of independently moving parts.
  [director] (f0) The 'free' arm never actually pumps past the hip — across all 24 sampled cells both hands stay parked in a tight band between sternum and belly button, so the signature downward 'dig' reads as a shrug, not a punch.
      FIX: Keyframe the free arm's elbow/fist to drive down to hip/thigh height (add real downward travel, not just a slight forearm rock), and hold the low extreme for a hair before the return so it registers as a hit on the beat.
  [director] (f21) The choreography brief calls for the working arm to swap sides every bar, but at frame 21 (second bar) the pose is nearly a carbon copy of frame 0/11 in silhouette and amplitude, so the side-swap accent is invisible at a glance and the whole loop reads as one repeating shuffle rather than two contrasting bars.
      FIX: Differentiate the bars — vary shoulder lean direction, add a beat of held contrapposto, or widen the stance asymmetrically on bar 2 so the cut between bars is legible even at thumbnail size.
  [director] (f16) The 'side-to-side weight commit' is choreographically supposed to be a torso/hip event, but the torso and shoulder line stay vertically centered and camera-facing through the whole phrase (frame 16 is representative) — only the feet cross-step a little, so the weight transfer doesn't survive in silhouette.
      FIX: Add lateral hip and shoulder translation/tilt synced to the leg cross-step so the character's whole axis leans off-center at each weight-commit extreme, not just the feet.
  [anatomy] (f0) Both arms hold an identical bent-elbow, fists-near-sternum configuration in every sampled frame (compare frame 0 to frame 16, 20 frames later — pose is pixel-for-pixel the same). The named move requires one paw pinned at the sternum while the OTHER arm pumps down past the hip on a shoulder dig, alternating sides per bar. Neither shoulder ever depresses/drives, and no wrist ever travels below the beltline.
      FIX: Break bilateral symmetry: keep one wrist clamped near the sternum as the anchor, and drive the other shoulder through depression + elbow extension so that hand travels past the hip line and back on each beat; mirror which arm pumps at the bar boundary (frame 16).
  [anatomy] (f5) In the widest stance frames (e.g. the deep diagonal lunge) both the front and back legs are drawn essentially dead straight from hip to ankle — full knee extension under a wide lateral weight-shift. A real weighted plant that wide requires knee flexion to lower the center of mass and absorb the lateral load; with the knee locked out, the pose reads as an ice-skating glide rather than a grounded weight commit, and is the classic geometry that stresses the ACL/MCL if a real body tried to load it this way.
      FIX: Add ~15-25 degrees of flexion to the loaded (front) knee at peak stance, with a quick rebound/absorb-and-recover curve timed to the beat so the weight-commit reads as landed rather than skated.
  [anatomy] (f4) The back foot is rotated to a steep angle relative to the hips (near-perpendicular to the direction of travel) to sell the cross-step, but the spine, ribcage and shoulder line stay perfectly square/frontal — there is zero axial rotation traveling up through pelvis and thorax. A foot pivoted that far while the tibia, pelvis and spine above it stay untwisted is not a mechanically connected kinetic chain; it reads as the ankle absorbing rotation the hip and spine should be sharing (a real ankle would be at sprain risk), and it means the choreography's 'plant-foot twist' has no visible twist above the shin.
      FIX: Couple pelvis and thoracic rotation to the foot-pivot angle each stance frame (e.g. 10-15 degree hip rotation, smaller counter-rotation at the shoulders/ribcage) so the twist visibly travels up the spine instead of terminating at the ankle.
  [rigging] (f5) A thin, disconnected outline stroke cuts diagonally across the jacket front near the sternum-pinned hand, floating independent of any filled shape (visible on both the left-pinned and right-pinned mirrored poses, and confirmed in the lit stage render too).
      FIX: Weld the sleeve ribbon's outline stroke to its fill geometry (union the stroke path with the mesh silhouette) instead of rendering them as separate passes, or add overlap at the fold so the seam edge doesn't peek through as a stray line.
  [rigging] (f2) The far-arm elbow collapses to a sharp black cusp/spike instead of a rounded bend at the point of maximum flex, breaking the suit's smooth silhouette.
      FIX: Clamp a minimum cross-section width (or insert an extra control vertex) at the elbow joint in the weighted mesh so the ribbon doesn't pinch to zero width at high bend angles.
  [rigging] (f0) A sliver of skin-toned cuff/hand pokes past the near jacket sleeve's outline at the hip, an interpenetration/occlusion-order break between the far arm and the torso.
      FIX: Fix arm draw-order/occlusion masking so the far arm's cuff renders fully behind the torso silhouette rather than leaking a spike past the jacket edge.

======================================================================
BUGA  mean 6.6  min 6

  VERDICT afrobeats: The lo-lo-lo sink and the bilateral peacock-bow BUGA hit are now the real move and read correctly to a Lagos eye, but the money moment is sold by the arms alone — face, chest, and hands stay flat, so it looks like a pose snap rather than a flaunt.
  VERDICT movement: The sink-and-present arc is real and musically placed, but the "hit" reads as generic jazz-hands rather than a Buga shoulder shrug, and the weight quality skates instead of sinking.
  VERDICT director: The hit pose has real charm (great surprised face, open arm silhouette) but the build-up reads as an idle hold rather than a countable three-stage sink, and the key pose has a tie-mesh glitch -- solid draft, not yet a sign-off.
  VERDICT anatomy: The groove reads fine in the round, but the signature "hit" collapses into a locked-elbow scarecrow T-pose with dead-straight, heel-raised legs — exactly where the shrug and the sink need to land, the joints go rigid instead of dynamic.
  VERDICT rigging: The three-count sink reads clean and well-graduated with no interpenetration, but the signature show-off present collapses into two dead-straight rod arms and a strap-like tail, so the move's one big accent is its weakest piece of rigging.
  [afrobeats] (f12) On the BUGA hit itself the face and head are byte-for-byte identical to the sunk 'lo' counts — same smile, same neutral head tilt (rootRotation only 0.003rad, i.e. nothing). 'Buga' means to show off/flex, and in the source video the flex is sold as much by the face (chin up, cocky eyes) as by the arms; here the cat's expression doesn't change when it hits the loudest pose in the phrase, so the moment reads as a mechanical arm-pop rather than a cat proudly showing off.
      FIX: Add a head/chin DanceJointKey on frame 12 (and mirrored 28): chin lifts a few degrees, brief eyebrow raise or eye-squint/smirk, held through 12-14 and releasing with the arm retract at 15-16 — the same pulse-and-release shape already used for the clavicle shrug.
  [afrobeats] (f12) The chest 'puff' on the hit is coded as chestScaleX:0.97 / chestScaleY:1.16 — narrower and taller — which visually stretches the torso upward rather than broadening it sideways. A proud peacock chest reads as WIDE across the shoulders/ribcage, not tall and thin; as rendered the cat looks like it's reaching/gasping upward rather than puffing its chest out.
      FIX: Invert the ratio for the hit key: chestScaleX ~1.08-1.12 (broaden), chestScaleY ~1.03-1.06 (mild lift only), so the silhouette visibly widens across the hit instead of elongating.
  [afrobeats] (f12) The peacock-bow paws stay clenched, flat fists through the entire opening transit and hold (frames 10-14) — no wrist roll or paw-open flourish accompanies the arm extension, so the signature pose lands as a stiff scarecrow/semaphore shape instead of a flourish. Nothing in the rig currently drives a hand rotation for buga (handR/handL are used only as IK targets, never given their own rotation key).
      FIX: Add a short wrist/paw rotation key on the handR/handL joints synced to the frame 10->12 opening transit (roll from knuckles-forward to a slight open/upward flick), so the paws visibly 'pop' open on the hit the way the chest and shoulders already do.
  [movement] (f10) The crouch phase (frames 0-10) is built as a forward skater-lunge — a fully extended back leg pushing the torso forward and down — which reads as a light, gliding travel step rather than a grounded, weighted 'lo-lo-lo' sink into the floor.
      FIX: Keep both feet roughly under the hips and bend both knees symmetrically (plié-style) as the torso drops, so the weight visibly loads straight down through both legs rather than gliding forward on one extended leg.
  [movement] (f12) The brief calls for a 'shoulder shrug on the hit,' but the accent at the beat is a fully symmetric two-arm fling to shoulder height (identical on both sides, no shoulder isolation) — it reads as a generic ta-da/jazz-hands pop, not the specific Buga shrug gesture, and the exact same canned pose is reused verbatim for the second hit (frame 28-30 mirrors frame 12-14 almost pixel-for-pixel).
      FIX: Add a fast, isolated shoulder hike (lead shoulder or both shoulders toward the ears) that snaps up and releases within 1-2 frames, distinct from and prior to the arm spread, and vary the second occurrence (e.g. opposite shoulder, or add a head tilt) so the two hits don't read as an identical copy-paste.
  [movement] (f13) At the moment of presentation the legs are still mid-stride (crossed, walking) in every sampled frame of the hold (12, 13, 14), so there is never a stopped, weighted silhouette for the audience to read — the 'show-off' has no still contrast against the continuous sink, and the true peak extension (headtop metric) lands on frame 13, a quarter-beat after the frame-12 downbeat, so the accent feels a hair behind the music rather than cracking on it.
      FIX: Plant both feet and hold the hip/leg silhouette static for at least 2 frames at the peak, and shift the fullest extension pose to land exactly on the frame-12/28 downbeat (or 1 frame earlier as anticipation) so the freeze reads as landing on the hit.
  [director] (f4) The 'lo-lo-lo' sink is real in the rig data (head/torso does descend roughly 13-14% of frame height from cell 2 to cell 8) but it's carried entirely by a continuous knee-bend/walk-cycle bob while the arms stay locked on the hips the whole time. On screen it reads as a generic idle sway, not three countable, accumulating drops -- there's no discrete pop or accent marking count 1, 2, 3.
      FIX: Stage the sink as three distinct held sub-poses (e.g., shoulder dip, then ribcage dip, then hip dip) and give each one its own small arm/hand accent (a knuckle press into the hip, a slight shoulder roll) so the eye can count three beats instead of perceiving one smooth bob.
  [director] (f16) Roughly two-thirds of the 32-frame loop (frames 0-11 and 16-23) is the same boxy hands-on-hips silhouette with only a few pixels of vertical drift between consecutive frames -- it plays as a held/idle pose rather than a performance, which is a lot of dead screen time before the payoff.
      FIX: Introduce a secondary silhouette change during the sink (progressive shoulder rotation, elbow flare, or head tilt building toward the hit) so frames read as distinct stages rather than a frozen walk-cycle loop.
  [director] (f12) On the presenting/shoulder-shrug hit pose -- the actual poster-frame moment, confirmed on two different characters in the lit render -- the necktie mesh balloons into a thick, asymmetric blob instead of hanging as a ribbon, which is a visible rigging artifact right at the shot's key frame.
      FIX: Clamp/stiffen the tie ribbon's weight falloff during wide arm-spread poses (or pin it closer to a fixed rest curve when shoulders rotate) so it doesn't stretch into a warped shape on the money frame.
  [anatomy] (f12) On the show-off 'present' hit, both arms extend as a single dead-straight ribbon from shoulder to fist with zero elbow break, mirror-symmetric, and held essentially unchanged across the whole beat — there is no visible shoulder elevation at all, so the choreographed 'shoulder shrug on the hit' simply isn't happening.
      FIX: Give the hit its own key: bend both elbows 15-25° off full extension, drive a genuine scapula/clavicle lift (both shoulders toward the ears) for 2-3 frames before releasing, and stagger the two arms slightly so the flourish doesn't read as a static T-pose.
  [anatomy] (f12) In that same wide hit stance both legs run hip-to-foot as straight, knee-locked lines while both heels are lifted off the ground — a real body cannot sink its center of mass that way, and a straddle with locked knees plus bilateral heel-lift is not a stable weight-bearing position, it reads as balancing on stilts rather than 'sinking' into the pose.
      FIX: Plant the front foot flat with a clearly flexed knee (20-30°) to carry the weight, and keep only the trailing foot's heel released; let the standing knee visibly bend so the pose looks grounded, not stilted.
  [anatomy] (f4) The first 'lo' of the progressive sink shows the working leg lifted knee-forward like a light jog stride while the torso leans forward and the head height barely drops from the upright starting pose — the intended descending weight-drop reads as a traveling prance step instead of a squat-sink, and the standing leg is left to balance a hip-height thigh lift with no compensating pelvic/lumbar counter-tilt shown.
      FIX: Keep the working foot low and let the pelvis/ribcage actually drop vertically frame to frame (visible head-height decrease across the three counts) rather than swinging the leg forward into a stride; add a slight posterior pelvic tilt to sell the counterbalance if any knee lift is kept.
  [rigging] (f12) On the presentation/shrug hit, both arms are perfectly straight lines from shoulder to fist with zero elbow bend, so the flourish reads as two rigid rods swinging open rather than a jointed, fleshed sleeve.
      FIX: Add a mid-ribbon elbow control point with 15-20 degrees of bend during the hold, and offset the left/right arm timing by a frame or two so the pose isn't a perfectly mirrored V.
  [rigging] (f8) During the hands-on-hips holds the tail renders as a flat, constant-width horizontal band lying across the hip and tangenting the fist's outline, reading as a belt/strap rather than a cat tail.
      FIX: Taper the tail mesh toward the tip and route its curl down/back so it clears the fist's silhouette instead of running parallel through it.
  [rigging] (f13) In the fully extended presentation pose the sleeve stays thin along its length then balloons abruptly into the round fist, giving a balloon-on-a-stick junction instead of a smooth forearm-to-hand taper.
      FIX: Add an intermediate taper/width key at the wrist so the ribbon widens gradually into the cuff and fist rather than jumping width in one segment.

======================================================================
POUNCINGCAT  mean 6  min 5.5

  VERDICT afrobeats: Solid Amapiano-flavored bounce and a genuinely grounded wide stance, but the "cat pounce/swipe" signature is a blink-and-miss-it accent buried inside what reads for 90% of the loop as a boxer's guard-and-gallop.
  VERDICT movement: A likable boxer-guard bounce-and-open with a nice on-beat catch, but it never actually delivers the "pouncing cat contrast phrase" it's billed as — the head sinks with the body instead of gliding level, the signature swipe opens both arms symmetrically instead of crossing the body, and the second bar is a copy of the first rather than a built phrase.
  VERDICT director: Clean holds and a genuinely level head over the base, but the 'two-bar phrase' is one bar copy-pasted, and the swipe/pounce beats are too tucked-in to read as decisive action at a glance.
  VERDICT anatomy: The compress-swipe-rebound phrasing and dead-level head are well conceived, but the swipe's arm topology and the compress's plant foot both fail plausibility at the two beats that define the move, and they repeat on both sides of the loop.
  VERDICT rigging: Competent, readable dance phrase with good tail secondary motion and a mostly level head, but a real geometry glitch on the tie during the arm-swipe and a slippery weight-bearing foot keep this below sign-off quality.
  [afrobeats] (f6) The cross-body paw swipe — the move's entire reason to exist — only appears as a single transient key per 4-beat window (around frame 6-8, mirrored near frame 20-22); the rest of the 32-frame loop is a held fists-up guard bouncing side to side. A Lagos/Accra viewer watching at speed would clock this as "cartoon henchman shuffle," not a cat swipe, because the signature shape never dwells long enough to register.
      FIX: Give the swipe an actual hold (4-6 frame dwell at the fully extended cross-body reach, like a hit-and-hold rather than a passing key) so the eye has time to read the diagonal paw line before it snaps back to the guard.
  [afrobeats] (f4) The clip's own comment promises a 'dead-level head riding over a gliding base' as the defining Amapiano contrast, and danceHeadBobScale is zeroed to kill head nod — but the head is still parented to a root/chest that sinks and squashes hard on the compress (chestScaleY drops to 0.64, root sinks), so the head visibly drops and pops back up in sync with the torso at exactly the moment the contrast needs to be sharpest.
      FIX: Add a counter-translation on neck/head that cancels the net vertical displacement coming from rootDy + chestScaleY during the compress/rebound keys, so the skull's world-Y stays pinned flat through the pounce, not just during the glide segments.
  [afrobeats] (f8) Even at the extended-reach frame, the hand reads as a closed fist thrown outward (boxer jab silhouette) rather than an open, clawed cat paw — so the one moment meant to sell 'pounce' instead reads as a punch, losing the character joke entirely.
      FIX: Swap the fist pose for a splayed/open-digit paw shape (or a visible claw flourish) specifically on the swipe frames so the silhouette unmistakably reads as a cat swat, distinct from the default closed-fist guard held everywhere else.
  [movement] (f4) The 'dead-level head riding over a gliding base' contrast never happens: the head sinks with the whole torso into the compress (head-top rises from y~143px to y~167px going into the crouch, then springs back up to y~112px on the rebound) — a ~50-55px vertical travel matching the torso, not a stabilized head over working knees.
      FIX: Decouple the head/upper-spine from the hip-knee compression in the rig: lock the head's world-Y within a few pixels while the knees/hips absorb the entire crouch depth, so the head visibly floats level while the base does the pumping.
  [movement] (f4) The 'cross-body paw swipe' is actually a bilateral, mirror-symmetric arm-open (both forearms swing out sideways together, like a reveal/guard stance) rather than one paw crossing the body's midline; the one true crossed-arm shape shows up later, folded into the rebound catch instead of reading as its own swipe accent.
      FIX: Commit one arm to a clear diagonal path from the opposite hip/rib up across the chest to the far shoulder while the other arm stays low and anchored, so the swipe reads as a single directional paw strike rather than a symmetric double gesture — this is also the character's default marketing pose, so the mismatch is highly visible.
  [movement] (f16) The 8-beat 'phrase' is a 4-beat loop played twice with no development: bar 2's poses (frames 16-31) are near-identical in shape, order and spacing to bar 1's (frames 0-15) — verified by direct pixel comparison of matched cells — so it reads as a groove loop mislabeled as a composed 2-bar phrase.
      FIX: Vary the second half — mirror the swipe to the opposite side, deepen the second compress/rebound for a build, or add a small net travel step — so the ear and eye register bar 2 as a development of bar 1, not a repeat.
  [director] (f16) The 8-beat phrase is not actually two bars of choreography — frames 16-31 are near-pixel-identical repeats of frames 0-15 (same arm, same leg, same tail curl, same head tilt at every matching beat). A 'phrase' that just loops its own first half twice reads as a 1-bar move stretched to fill 2 bars, which is exactly the visual monotony a director flags on first watch.
      FIX: Mirror the second bar (swipe with the opposite paw/leg) or vary the rebound accent on repeat 2 — different tail flick timing, a slightly higher pop, or a head-tilt flip — so beats 5-8 read as a response to beats 1-4 rather than a carbon copy.
  [director] (f3) The cross-body 'paw swipe' has no visible arc: the arm is already at full cross-body extension by frame 0 and barely changes through frames 1, 3, and 4 (a full beat of near-static hold before the rebound). The signature gesture of the move never actually travels across the body on screen.
      FIX: Insert a clear mid-travel key (paw starting near the chest/hip and sweeping out) inside frames 1-3 so the eye reads a swing before the hold, instead of a pose that simply appears already-extended.
  [director] (f7) At the rebound/pounce apex (the highest-energy pose in the loop) the leading paw folds in tight against the cheek/chin rather than extending outward, so the pose silhouettes as a coy or thinking gesture instead of a pounce payoff — the one moment built to pop on a small screen under-delivers.
      FIX: Push the paw further up and away from the face at the apex (claw open, arm extended past the head's silhouette) so the pounce reads as an outward strike rather than an inward tuck.
  [anatomy] (f8) At the swipe's apex both arms' elbow bends collapse onto almost the same point on the sternum/collar, and the raised 'guard' arm's upper-arm segment is compressed to near zero length so its fist appears to sprout directly out of the neck/collar rather than out of its own shoulder. Two shoulders spaced apart on the torso cannot both route their elbows through one spot without one limb passing through the other; this repeats in mirror form on the second swipe (frame 24). A viewer scrubbing this frame sees the crossing arm and the cocked guard arm braid into one tangled shape at the chest instead of two readable limbs, at the exact instant meant to sell the 'paw swipe' accent.
      FIX: Stagger the two elbow pivots in depth/height (drop the swiping arm's elbow lower on the ribcage, keep the guard elbow tucked but restore a visible upper-arm segment linking the fist back to its own shoulder) so the sleeves never fuse into one silhouette.
  [anatomy] (f2) In the deep compress the weight-bearing front foot renders with its plantar/tread face toward camera while the shin above stays in the sagittal plane, i.e. the ankle reads as rolled into inversion under load rather than neutral or plantarflexed onto the ball of the foot; this repeats at frames 4, 18 and 20. A real ankle cannot bear crouch weight in that rolled position without spraining, and it lands on the move's other signature beat, the crouch itself.
      FIX: Rebuild the foot mesh for this pose so the shoe's lateral profile (not the sole) faces camera, or reduce the ankle-roll angle so the foot reads flat/braced under the bent knee.
  [anatomy] (f9) The ribcage/torso block is an identical rigid shape in every frame of the phrase: comparing the neutral pose, the deepest compress, and the swipe apex shows zero spinal rotation, lateral flexion, or hip-hinge, so all visible motion is isolated to shoulders, hips and knees. At frame 9 the cross-body swipe's torque visibly comes from the shoulder alone with no thoracic counter-rotation, and at frame 4 the deep crouch shows no forward hip-hinge/lumbar give, so the spine reads as an inert box with limbs bolted on, undercutting the 'contrast phrase' idea of one coordinated whole-body gesture.
      FIX: Add roughly 5-10 degrees of ribcage counter-rotation opposite the swinging arm during frames 7-10 (and their mirror), and a slight forward torso lean/hip-hinge through the compress frames 2-5, while still keeping the head dead-level via a separate neck counter-rotation.
  [rigging] (f8) When the near arm swings across the chest, the tie knot doesn't stay occluded/deformed correctly under the sleeve mesh — it pops out as a floating solid red disc sitting on top of the crossing arm instead of reading as a knot tucked behind fabric. It looks like a loose button, not a tie.
      FIX: Add the tie knot to the arm's occlusion/paint-order group (or clip it to the torso layer) so it renders behind the crossing forearm ribbon through the full swipe, and taper its shape so it doesn't degrade into a flat circle at extreme overlap angles.
  [rigging] (f19) On the weighted/compressed leg during the paw-swipe extension, the trailing foot rolls onto its outer edge with the white sole facing camera and the heel floating — there's no clear weight-bearing edge, so the plant reads as a slip/pop rather than a controlled glide contact.
      FIX: Lock the grounded foot's rotation to a consistent contact edge (heel or full sole) for the duration of the weight-bearing hold, and drive the ankle roll from the hip/leg IK rather than letting it free-rotate independently frame to frame.
  [rigging] (f0) The ribbon-sleeve arms stay a near-constant width with almost no visible bend or taper at the elbow when fully extended, so the cross-body swipe reads as one stiff paddle rotating from the shoulder rather than an articulated paw striking out — it undercuts the snap the swipe needs.
      FIX: Add a slight width pinch and a visible direction break at the elbow control point on the ribbon mesh so the silhouette shows two distinct limb segments during extension, not one flat banana shape.
